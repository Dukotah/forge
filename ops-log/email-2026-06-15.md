# EMAIL FALLBACK — Gmail auth required, not sent
> To: dukotah@gmail.com  
> Subject: Copper Bay Labs ops — 2026-06-15 — DepCheck npm audit remediation guide shipped

---

## 1. What shipped this run

**New guide page — DepCheck:**  
`depcheck/guides/fix-npm-audit-vulnerabilities.html`  
Live (after GH Pages build): https://labs.copperbaytech.com/depcheck/guides/fix-npm-audit-vulnerabilities.html

Targeting the query **"how to fix npm audit vulnerabilities"** — a developer who has just run `npm audit` and needs to know what to actually do about the output. ~900 words of genuinely actionable content, no fluff:

- Severity table to help readers triage the report
- Step 1: `npm audit fix` (safe semver-compatible path)
- Step 2: Manual targeted upgrades for major-version bumps
- Step 3: Transitive dependencies — upgrading the parent, or forcing a safe version with the `overrides` field (npm 8.3+) / `resolutions` (Yarn) / `pnpm.overrides` — with a callout about breakage risk
- Step 4: No fix available yet — checking GitHub issues, assessing reachability, documenting accepted risk
- Step 5: Replace abandoned packages — DepCheck CTA for finding unmaintained deps beyond CVEs
- Prevention in CI — `--audit-level` flag, lockfile discipline, scheduled `npm outdated` reviews
- 6-question FAQ with JSON-LD FAQ schema

Also updated:
- `depcheck/sitemap.xml` — new guide URL added at priority 0.8
- `depcheck/index.html` — "Fix guides" added to header nav + footer nav; inline guide link added to the "Found a vuln — now what?" FAQ answer

Commit: `0900df7` on `dukotah/depcheck` main

This is the 3rd fix-intent guide in the rotation:
- 2026-06-11: ExposureCheck → "how to fix exposed .env file"
- 2026-06-13: LeakCheck → "accidentally committed api key to github"
- 2026-06-15: DepCheck → "how to fix npm audit vulnerabilities"

---

## 2. Owner browser handoff queue

| # | Task | Status |
|---|------|--------|
| 1 | Confirm `contact@copperbaytech.com` RECEIVES email (send a test from your own inbox) | **UNCONFIRMED** — blocks task (c) lead CTA rollout |
| 2 | Google Search Console: verify `dukotah.github.io` and submit each tool's sitemap.xml. New sitemap to submit: `https://labs.copperbaytech.com/depcheck/sitemap.xml` (also ExposureCheck and LeakCheck sitemaps from prior runs if not yet submitted) | Not started |
| 3 | Post prepared launch posts from `forge/marketing/` to your own accounts | Ongoing |
| 4 | (Optional) Set up free Cloudflare Web Analytics; paste snippet for installation | Not started |
| 5 | **Gmail auth:** The Gmail MCP connector needs a one-time OAuth approval. Open `https://api.anthropic.com/authorize?response_type=code&client_id=82deb415-04ab-48b8-b31c-3245c38d6501&code_challenge=hHiylbvsItCzb91fBq3g8FzLdI2Hsx2kdhawPsSOx7I&code_challenge_method=S256&redirect_uri=http%3A%2F%2Flocalhost%3A3118%2Fcallback&state=_37itafHrsvxZqQYwL8kxy3QJ3x_aafQJim4BylFSZo` in your browser, complete the flow, then paste the callback URL back into a session so the connector can authenticate. After that, future runs will email you directly. | Needs owner action |

---

## 3. Blocked

- **Task (c) — lead CTA rollout** across depcheck/hardencheck/exposurecheck: blocked until `contact@copperbaytech.com` delivery is confirmed (guardrail: don't wire leads into a possibly-dead inbox).
- **Email delivery**: Gmail MCP requires interactive OAuth. Writing this to file instead. Next run will retry if auth has been completed.

---

## 4. Next guide candidates (for next run)

- HardenCheck → "how to add Content-Security-Policy header" — high-intent, CSP is notoriously confusing
- ShipSafe → "is my Lovable / Webflow / Framer site WCAG compliant" — targets no-code builder audience
