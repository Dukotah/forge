# Copper Bay Labs — Ops Log — July 2026

## 2026-07-25

**Batch: SEO content (priority a) — ExposureCheck third guide: how to prevent exposed files on your website**

### What shipped

**New page:** `exposurecheck/guides/prevent-exposed-files-website.html`
Live URL: https://labs.copperbaytech.com/exposurecheck/guides/prevent-exposed-files-website.html
Git commit: `72b39dd` pushed to `dukotah/exposurecheck` main

A ~1,000-word prevention guide targeting the queries **"how to prevent exposed files on website"**, **"prevent .env file exposure"**, **"website file exposure prevention"**, and **"how to keep .env file private"** — a developer or site owner who wants to stop sensitive files from becoming publicly accessible in the first place, rather than finding or fixing them after the fact. This is the third and final guide in ExposureCheck's three-moment lifecycle: fix (post-incident) → detect (proactive audit) → **prevent (upstream prevention)**.

**Article structure (all content is genuine, technically accurate):**
- Root causes section (4): webroot equals project root; secrets committed to version control; no server-side path blocking; source maps left enabled in production. Framing: understanding the failure mode is the fastest path to a structural fix.
- Webroot separation — the single most structurally effective prevention; project root vs. webroot distinction; VPS rsync-only-build-output pattern; symlink zero-downtime-deploy callout; why Netlify/Vercel enforce this by default
- Secrets management — .gitignore patterns for all .env variants; platform native secret stores (Vercel, Netlify, GitHub Actions, VPS/Docker) in a 4-card grid; callout: platform stores prevent future exposure but don't retroactively fix committed history (link to fix guide Step 4)
- Server deny rules — the "blocking sensitive paths is your safety net" framing; per-platform config snippets in a 5-card grid: Nginx (location ~ /\. { deny all; }), Apache (.htaccess FilesMatch blocks), Netlify (_redirects), Vercel (vercel.json redirects), Cloudflare Pages (_redirects); GitHub Pages callout (no server-side rules; prevention relies on keeping files out of repo + Cloudflare proxy)
- Source map prevention — why source maps leak original source code; per-build-tool config: Vite (build.sourcemap: false), webpack (devtool: false), Create React App (GENERATE_SOURCEMAP=false), Next.js (productionBrowserSourceMaps: false); post-build DevTools verification
- Database backup hygiene — never write to webroot; managed DB backup features vs. manual SQL dumps; streaming-to-object-storage pattern (pg_dump | gzip | aws s3 cp); .gitignore backup extensions
- 6-item pre-deploy checklist (styled checklist cards): webroot contains only build output; no .env tracked by git; source maps disabled; server deny rules in place; no backup files in webroot; run ExposureCheck after deploy
- ExposureCheck CTA button
- FAQPage JSON-LD schema (7 questions): single most effective thing; .gitignore vs webroot; Nginx .git block; disabling source maps; where to store secrets; GitHub Pages limitation; database backup location

**Also updated:**
- `exposurecheck/sitemap.xml` — added prevention guide URL at priority 0.8, lastmod 2026-07-25
- `exposurecheck/index.html` — "Prevention" added to header nav; "Prevention guide" added to footer nav with all three guides now listed
- `exposurecheck/guides/how-to-check-website-exposed-files.html` — "Prevention" added to header nav; "Prevention guide" added to footer nav; three-way cross-link structure complete
- `exposurecheck/guides/fix-exposed-env-file.html` — "Prevention" added to header nav; "Prevention guide" added to footer nav; three-way cross-link structure complete

### Why this batch

ExposureCheck had two guides covering two distinct search moments: post-incident fix (the panicked developer who just found their .env exposed) and proactive audit (the systematic developer running a pre-launch check). The prevention guide fills the third moment: the developer who wants to eliminate the root causes so neither of the first two situations arises. These three moments represent different developer mindsets — incident response, security hygiene, and deployment workflow — and reach different stages of the same developer's journey.

Key differentiators vs. generic "website security" articles:
1. **The webroot separation frame** — almost all other "how to protect .env" articles focus on .gitignore and never explain that .gitignore is irrelevant to what the web server serves. The guide makes this distinction explicit: "a .env file that was excluded from git can still be copied into the webroot by a deploy script." Most developers don't know this.
2. **Platform-specific server deny rules** — a Nginx `location ~ /\. { deny all; }` block covers all hidden-path exposures with a single rule; most articles give either no config snippets or one platform only. The 5-card grid covers the full range of hosting contexts.
3. **The GitHub Pages callout** — GitHub Pages has no server-side deny rules; the only protection is keeping files out of the repository. This limitation is widely unknown and frequently the source of exposure on Pages-hosted sites.
4. **The "platform stores prevent future exposure but not history" callout** — developers who move secrets to Vercel/Netlify env vars often believe they've fully remediated a past commit. The guide is explicit that git history scrubbing is also required and links to the fix guide Step 4.

The three ExposureCheck guides now cover the full exposure lifecycle: found something → detect what else is there → prevent future incidents. Every guide links to its neighbours at the natural handoff point in the reader's workflow.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates

- **ShipSafe → launch posts (task d)** — ShipSafe has three guides (ADA checklist, WCAG 2.2 changes, AI-site compliance) covering three distinct search moments. No `launch-shipsafe.md` exists yet. Good candidate for the next task (d) run; the business-risk + developer audience split makes it interesting across multiple channels.
- **HardenCheck → fifth guide: "how to implement Content Security Policy"** (task a) — CSP is the most complex header in HardenCheck's set and the most-queried implementation topic. An implementation guide covering the `nonce-`/`hash-`/`'strict-dynamic'` pattern, report-only deployment workflow, and common breakage patterns would fill a high-volume developer query and differentiate from the existing four HardenCheck guides.
- **DepCheck → launch posts (task d)** — DepCheck now has three guides (fix npm audit vulnerabilities, full pre-deploy audit, npm typosquatting detection). No `launch-depcheck.md` exists. The typosquatting angle is genuinely interesting for Show HN and r/netsec; the full three-guide set makes it credible to showcase.

---

## 2026-07-23

**Batch: SEO content (priority a) — ComplyKit third guide: do I need a cookie consent banner?**

### What shipped

**New page:** `complykit/guides/cookie-consent-banner-requirements.html`
Live URL: https://labs.copperbaytech.com/complykit/guides/cookie-consent-banner-requirements.html
Commit: 1cdde34a pushed to `dukotah/complykit` main

A ~900-word compliance guide targeting the queries **"do I need a cookie consent banner"**, **"GDPR cookie consent requirements"**, **"cookie consent banner requirements"**, and **"what cookies require consent"** — a developer or site owner who has heard about GDPR cookie banners but isn't sure whether the requirement applies to them, which cookies actually need consent, and what valid consent looks like.

**Article structure (all content is genuine, legally accurate):**
- Which laws require cookie consent: ePrivacy Directive (EU), PECR (UK), GDPR (defines valid consent), CCPA/CPRA (opt-out for advertising, not prior consent)
- Essential vs. non-essential cookies: visual cards with concrete examples; session auth, CSRF tokens, language/currency prefs are essential; GA4 analytics cookies, advertising DoubleClick/Facebook, retargeting pixels, A/B testing cookies are non-essential requiring consent; the "can you remove it and still serve the user?" test
- What valid consent means under GDPR Art. 4(11): freely given, specific, informed, unambiguous, withdrawable; per-category requirement (analytics ≠ advertising)
- 5 invalid consent patterns with regulatory citations: pre-ticked boxes (EDPB 05/2020), cookie walls (Planet49 C-673/17), scrolling/continued browsing as consent, consent harder to withdraw than to give, vague/bundled categories; CNIL fines against Google (€150M) and Facebook (€60M)
- US-only sites: no federal consent mandate, but CPRA opt-out required for advertising cookies shared with ad networks; Plausible/Fathom as cookieless avoidance path
- What a valid banner must contain: 7 required elements (layered consent, categories, granular controls, reject-all button equal prominence to accept-all, no dark patterns, remember choice, explain withdrawal)
- FAQ + FAQPage JSON-LD schema (7 questions)

**Also updated:**
- `complykit/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-07-23
- `complykit/index.html` — "Cookie consent" link added to header nav and footer nav
- `complykit/guides/do-i-need-a-privacy-policy.html` — "Cookie consent" added to header nav and footer nav; nav label updated from "Fix guides" to "Do I need a policy?"; inline cross-link in FAQ answer ("from your cookie-consent banner")
- `complykit/guides/what-should-a-privacy-policy-include.html` — "Cookie consent" added to header nav and footer nav; inline cross-link in Section 6 cookie disclosure paragraph ("cookie banner with genuine choice")

### Why this batch

ComplyKit had two guides covering two distinct search moments: the threshold decision ("do I need a privacy policy?") and the contents checklist ("what should it include?"). Cookie consent is a third, distinct search moment — the builder who has dealt with the privacy policy but doesn't know whether their GA4 or advertising cookies require an opt-in banner before they fire. The third guide completes the natural three-question journey for ComplyKit's core user: (1) do I need a policy? → (2) what goes in it? → (3) do I need a cookie banner too?

Key differentiators: (1) the essential vs. non-essential distinction with the "remove it, can the user still complete their task?" test — a concrete decision rule; (2) 5 invalid consent patterns with specific regulatory citations (EDPB 05/2020, Planet49 C-673/17, CNIL fine amounts) — gives developers evidence to push back on dark patterns; (3) honest US-only analysis (no federal mandate, CPRA opt-out trigger for advertising cookies); (4) per-category consent requirement explicitly stated (analytics and advertising are separate consent decisions).

The three ComplyKit guides now cover the full compliance journey from first question to implementation. All three cross-link to each other.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates

- **ExposureCheck → third guide: "how to prevent exposed files on your website"** (task a) — prevention-intent guide completing the three-moment set for ExposureCheck (fix → detect → prevent). Analogous to LeakCheck's pre-commit guide.
- **ShipSafe → launch posts** (task d) — ShipSafe has three guides (ADA checklist, WCAG 2.2 changes, AI-site compliance) and a broad business+developer audience split. Good candidate for next task (d) run.
- **HardenCheck → fifth guide: "how to implement Content Security Policy"** (task a) — CSP is the most complex header in HardenCheck's set; an implementation guide covering the `nonce-`/`hash-`/`'strict-dynamic'` pattern and report-only deployment workflow would fill a high-volume developer query.

---

## 2026-07-21

**Batch: Task (d) — HardenCheck launch posts**

### What shipped

**New file:** `forge/marketing/launch-hardencheck.md`

One-paste-ready launch posts for HardenCheck across five channels. This is the second per-tool launch kit (following `launch-invoicequick.md` from 2026-07-15). HardenCheck was chosen because it now has the most guides of any tool in the suite (4: CSP, HSTS, XFO vs frame-ancestors, X-Content-Type-Options nosniff), which makes it the most credible to showcase; its developer audience is well-served by Product Hunt and Show HN; and the paste-mode / cookie-grading angles are genuinely interesting for the technical communities where this gets the most traction.

**Contents of `launch-hardencheck.md`:**

1. **Product Hunt** — tagline + full description (what each header check does and why, paste mode advantage, cookie flag grading angle, platform fix snippets) + first comment (FAQ on comparing to securityheaders.com, what an A grade means and doesn't, paste mode privacy, using paste mode with auth'd endpoints). PH topics: Developer Tools, Security, Web Development, Open Source.

2. **Show HN** — technical-first framing. Lead with the CORS problem (why URL-based header scanners can produce false grades when proxies strip headers) and how paste mode solves it. Implementation reflection questions in the first comment: whether per-cookie flag grading is the right granularity, whether A-F is a misleading simplification, and the CORS proxy disclosure tradeoff.

3. **r/SideProject** — "I built this" framing. Both modes explained. Genuine invitation for feedback on missing checks.

4. **r/webdev** — Showoff Saturday version (concise) + one-liner for answer-where-asked threads.

5. **r/indiehackers** — founder narrative: the consistent gap in indie apps (no CSP, no HSTS, cookie flags missing), the CORS-proxy problem I'd rethink, the cookie grading angle, four guides. Honest about limitations.

**Also included:**
- AlternativeTo / SaaSHub listing guidance (alternatives to list as, tags)
- Answer-where-asked text for organic thread engagement in r/webdev, r/devops, r/sysadmin, r/nextjs, Stack Overflow
- r/netsec note: direct tool link posts will be removed by mods; the four guides are the right content to share there, referenced in relevant technical discussions
- Timing notes: PH (Tue/Wed 12:01 AM PT), Show HN (1-2 days later, weekday morning ET), r/SideProject (same week), r/webdev Showoff Saturday (any Saturday), r/indiehackers (after PH/HN when there's feedback to reference)
- What-to-log section for recording metrics back to `metrics-ledger.md`

### Why HardenCheck for task (d) this run

Task (d) has been the first-listed "next candidate" for two consecutive runs (2026-07-17 and 2026-07-19) after the last task (d) batch on 2026-07-15. HardenCheck was chosen over ShipSafe for two reasons: (1) it has more guides (4 vs 3), making it the most content-credible tool to showcase right now; (2) the paste-mode and cookie-grading angles are technically interesting for Show HN and the developer audiences who would share this, whereas ShipSafe's ADA/WCAG angle is more compelling for a slightly different audience. ShipSafe is the natural next task (d) target.

The decision to target developer audiences (Product Hunt, Show HN, r/SideProject) rather than a broader audience reflects HardenCheck's actual user: someone who knows what nginx is and has deployed a site, not a business owner who wants a compliance report. The tool talks to developers in its own UI; the launch posts should match.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing Pro waitlist mailto in hardencheck/index.html untouched.
- **Auto-posting** — forbidden. All posts in `launch-hardencheck.md` are for the owner to fire from their own accounts.

### Next candidates

- **ShipSafe → launch posts (task d)** — ShipSafe has three guides and a broad business/developer split in its audience. The ADA checklist + WCAG 2.2 + AI-site-compliance set covers three distinct search moments; the combination makes ShipSafe credible to showcase. Good candidate for the next task (d) run after ShipSafe has had more time indexed.
- **ComplyKit → third guide: "do I need a cookie consent banner?"** (task a) — "do I need a cookie consent banner", "GDPR cookie consent requirements", and "what is an implicit consent cookie banner" are high-volume compliance queries; the three-guide set (threshold → what to include → cookie consent implementation) fills three distinct search moments for ComplyKit. Strong candidate for the next SEO content run.
- **ExposureCheck → third guide: "how to prevent exposed files on your website"** (task a) — prevention-intent guide completing the three-moment set for ExposureCheck (fix → detect → prevent). Analogous to LeakCheck's pre-commit guide which performed well structurally.

---

## 2026-07-19

**Batch: SEO content (priority a) — DepCheck third guide: what is npm typosquatting and how to detect it**

### What shipped

**New page:** `depcheck/guides/npm-typosquatting-how-to-detect.html`
Live URL: https://labs.copperbaytech.com/depcheck/guides/npm-typosquatting-how-to-detect.html
Git commit: `d90c30f` pushed to `dukotah/depcheck` main

A ~950-word detection guide targeting the queries **"npm typosquatting"**, **"what is npm typosquatting"**, **"typosquatted npm package"**, **"how to detect npm typosquatting"**, and **"npm install typosquatting attack"** — a developer who has heard about typosquatting but doesn't fully understand the attack model, or who wants a concrete pre-install verification workflow. This is the third distinct guide in DepCheck's set, filling the standalone typosquatting deep-dive that the existing audit guide (Step 4) explicitly points toward but doesn't fully cover.

**Article structure (all content is genuine, technically accurate):**
- How the attack works: the two-stage model (wait for typo → execute at install time via postinstall script); timing callout (code runs during npm install, not at runtime — before your editor even opens)
- Documented examples: the 2017 crossenv campaign (crossenv, babelcli, and others reported by Nick Sweeting) as the canonical well-documented example; ongoing automated campaigns with pattern description; comparison table showing what npm audit catches vs. what name-similarity scanning catches (the core differentiator)
- Three red-flag signal cards: name similarity, unexpected install script, publisher account signals — with explanation of why no single signal is conclusive and the combination is what matters
- Five-step pre-install verification workflow (steps, not a checklist): (1) check npmjs.com download count before typing the name; (2) read the postinstall script before it runs using `npm pack`; (3) check the publisher account age and package count; (4) verify the repository link; (5) use `npm info` to fetch metadata without running anything
- Audit existing package.json: DepCheck paste-scan as the recommended full sweep; `npm info` as a manual spot check for a specific suspect package; lockfile review callout (sha512 integrity hashes)
- Team enforcement: lockfile commit + PR review policy; `npm ci` (not `npm install`) in CI; `--ignore-scripts` / `.npmrc` option with honest caveat about native addons that legitimately need postinstall
- "Lockfile confusion attacks" warn callout — the more sophisticated variant targeting lockfile modification via social engineering
- Cross-link to the full pre-deploy audit guide as the natural next step
- FAQ/FAQPage JSON-LD schema (7 questions): what is typosquatting; does npm audit catch it; how do credentials get stolen; documented examples; how to check postinstall scripts; audit existing deps; typosquatting vs. dependency confusion (the adjacent term searchers often conflate)

**Also updated:**
- `depcheck/sitemap.xml` — new guide URL at priority 0.8, lastmod 2026-07-19
- `depcheck/index.html` — "Typosquatting" added to header nav and footer nav
- `depcheck/guides/audit-npm-packages-security.html` — inline cross-link added in Step 4 typosquatting section pointing to the new guide; "Typosquatting" added to header and footer nav
- `depcheck/guides/fix-npm-audit-vulnerabilities.html` — "Audit guide" and "Typosquatting" added to header and footer nav; three-way cross-link structure complete across all three DepCheck guides

### Why this batch

The existing audit guide (Step 4) mentions typosquatting in a single paragraph and stops there — it describes what typosquatting is at a surface level but gives no pre-install verification workflow, no documented examples, and no team enforcement guidance. The new guide fills the standalone search moment: the developer who specifically searches "npm typosquatting" or "how to detect typosquatted package" wants more than a paragraph — they want to understand how the attack model actually works before deciding whether to care about it, and then they want concrete steps.

Key differentiators vs. generic "npm typosquatting" articles:
1. The `npm pack` approach for reading postinstall scripts before running them — almost no other article mentions this workflow; most just say "check the scripts field on npmjs.com" without explaining how to inspect the actual code without executing it
2. The timing callout — "exfiltration completes during `npm install`, not at runtime" — is the most important thing a developer needs to understand about why this attack is so dangerous; it's commonly understated
3. The typosquatting vs. dependency confusion distinction (FAQ) addresses the term searchers often conflate; many developers encounter "dependency confusion" in security reports and don't know how it differs from typosquatting
4. The `--ignore-scripts` with honest caveats (native addons, binary downloads) — most articles either oversell it as a silver bullet or don't mention it at all

The three DepCheck guides now cover three distinct search moments and attack types: CVE remediation (fix) → multi-risk pre-deploy audit (audit) → typosquatting-specific supply-chain attack (this guide). All three cross-link to each other.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next candidates

- **Task (d) rotation → ShipSafe or HardenCheck launch posts** — ShipSafe has three guides; HardenCheck has four. `launch-invoicequick.md` was produced 2026-07-15 and no ShipSafe or HardenCheck launch kit exists yet. HardenCheck has the most guides (4) and targets a developer audience with clear pain (scanner output); ShipSafe targets a broader business-risk audience. Either is a strong candidate.
- **ComplyKit → third guide: "cookie consent banner requirements"** — "do I need a cookie consent banner" and "GDPR cookie consent requirements" are high-volume compliance queries; this is the third distinct search moment for ComplyKit (threshold decision → what to include → how to implement cookie consent). Neither existing ComplyKit guide covers it.
- **ExposureCheck → third guide** — ExposureCheck has two guides (fix + audit); a third covering "how to prevent exposed files on your website" (the prevention moment, analogous to LeakCheck's pre-commit guide) would complete the three-moment set.

---

## 2026-07-17

**Batch: SEO content (priority a) — LeakCheck third guide: how to set up pre-commit secret scanning**

### What shipped

**New page:** `leakcheck/guides/prevent-committing-secrets-pre-commit.html`
Live URL: https://labs.copperbaytech.com/leakcheck/guides/prevent-committing-secrets-pre-commit.html
Git commit: `b2e41ee` pushed to `dukotah/leakcheck` main

A ~1,000-word prevention guide targeting the queries **"how to set up pre-commit secret scanning"**, **"git hook prevent committing API keys"**, **"gitleaks pre-commit hook setup"**, and **"pre-commit hook for secrets team"** — a developer or tech lead who wants to stop secrets from entering git history at all, rather than finding and rotating them after the fact. This is the third distinct search moment in LeakCheck's guide set: fix (incident) → detect (audit) → prevent (pre-commit).

**Article structure (all content is genuine, technically accurate):**
- Why prevention beats remediation — the cost of history rewriting vs. a sub-second hook; the bots-harvest-in-seconds reality for public repos
- Three-approach comparison table (pre-commit framework / Husky / raw hook) with rows for: committed to repo, auto-installs for teammates, extra deps
- Approach cards with one-sentence tradeoffs above the table for scanning readers
- **pre-commit framework** (recommended): install gitleaks + pre-commit, .pre-commit-config.yaml with pinned rev, `pre-commit install`, manual test with `echo "sk_live_..."` staged file, `pre-commit autoupdate` tip callout
- **Husky** (Node.js projects): `npm install --save-dev husky`, `npx husky init`, `.husky/pre-commit` shell script, warn callout about `NODE_ENV=production` CI behaviour (Husky skips in CI by design; use the GitHub Actions gitleaks job for CI)
- **Raw git hook**: the `cat > .git/hooks/pre-commit` shell script with a useful error message; workaround for sharing via `scripts/hooks/` tracked directory
- **Allowlist config** with `.gitleaks.toml` example covering path exclusions, regex patterns for placeholders, and SHA-level exemptions for historical false positives; warn callout against broad path exclusions on directories that also contain real credentials
- **Team enforcement**: four strategies (CI gate, pre-commit framework over raw hook, onboarding script integration, `pre-commit run --all-files` in CI for parity); LeakCheck paste-from-staged-diff tip callout
- **Coverage gap table**: 6 scenarios — what the hook catches, what requires `gitleaks detect`, what requires CI, what requires ExposureCheck post-deploy
- Links to detection and fix companion guides
- FAQPage JSON-LD schema (7 questions): `--no-verify` bypass; staged-only scanning scope; commit speed impact; pre-commit vs raw hook distinction; GUI client compatibility; monorepo support; allowlist for test fixtures

**Also updated:**
- `leakcheck/sitemap.xml` — added new guide URL at priority 0.8, lastmod 2026-07-17
- `leakcheck/index.html` — updated footer nav to add "Prevention guide" link; updated "Can I run this in CI?" FAQ answer to link to both the prevention and detection guides
- `leakcheck/guides/accidentally-committed-api-key-github.html` — detection and prevention guide links added to footer nav; three-way cross-link structure complete
- `leakcheck/guides/scan-git-repository-for-secrets.html` — prevention guide link added to footer nav; three-way cross-link structure complete

### Why this batch

LeakCheck had two guides covering two distinct search moments: the panic fix (accidentally committed, need to rotate + scrub) and the proactive audit (want to scan the full history). The prevention guide fills the third moment: the developer who wants to stop secrets from ever entering history. These three moments are all high-intent but represent different mindsets — incident response, security hygiene, and workflow tooling — which means they reach different searchers and different stages of the same developer's journey.

The **team rollout problem** is the primary differentiator. Almost every other "gitleaks pre-commit hook" article shows how to install it for a single developer (the raw git hook script) and stops there. That approach does not scale: .git/hooks/ is excluded from git tracking, so every developer must install the hook manually. The pre-commit framework and Husky approaches solve this by putting the hook configuration in a committed file that is shared via git — the guide explains why this matters and how each approach handles the auto-install differently. The Husky section also includes the `NODE_ENV=production` callout, which is the most common gotcha when developers expect Husky to run in CI (it intentionally does not).

The three LeakCheck guides now form a complete, interlocking lifecycle: found something → detect what else is there → prevent future incidents. Every guide links to its neighbours at the natural handoff point in the reader's workflow.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next candidates

- **DepCheck → third guide: "what is npm typosquatting and how to detect it"** — the second DepCheck guide (audit-npm-packages-security) mentions typosquatting in Step 4 but does not cover it as a standalone deep-dive. The attack vector is concrete and shareable; DepCheck's typosquatting heuristic is a genuine differentiator vs. npm audit.
- **Task (d) rotation → ShipSafe or HardenCheck launch posts** — ShipSafe has three guides (the most of any tool); HardenCheck has four. Either would be a credible next launch kit. No `launch-shipsafe.md` or `launch-hardencheck.md` exists yet; `launch-invoicequick.md` was produced 2026-07-15.
- **ComplyKit → third guide: "cookie consent banner requirements"** — ComplyKit generates a cookie consent banner; the query "do I need a cookie consent banner" and "GDPR cookie consent requirements" are high-volume compliance queries that neither existing ComplyKit guide covers. Third distinct search moment for ComplyKit users.

---

## 2026-07-15

**Batch: Task (d) — InvoiceQuick launch posts**

### What shipped

**New file:** `forge/marketing/launch-invoicequick.md`

Prepared one-paste-ready launch posts for InvoiceQuick across six channels. This is the first per-tool launch kit (no `launch-*.md` files existed before this run). InvoiceQuick was chosen because it has the broadest non-developer audience in the suite (freelancers, contractors, small-business owners globally), backs up with two solid SEO guides, and this task was deferred for multiple consecutive runs.

**Contents of `launch-invoicequick.md`:**

1. **Product Hunt** — tagline + full description (privacy angle, zero-friction, client-side implementation) + first comment to post immediately after launch (FAQ pre-empting legal validity, print-to-PDF mechanics, .json reload, VAT questions). PH topics: Freelance Tools, Productivity, Finance, Business Tools.

2. **r/SideProject** — freestanding "I built this" post. Honest framing, no fabricated metrics. Technical details on why browser print-to-PDF was chosen over a server renderer.

3. **r/freelance** — longer post targeting InvoiceQuick's actual end audience. Leads with the privacy/friction pain point (client data in a random SaaS database). Includes guide links.

4. **Show HN** — technical-first framing (print-to-PDF rationale, zero infrastructure, .json feature, LocalStorage autosave) with a first comment pre-empting the common HN questions (legal validity, why not jsPDF, why not Wave/InvoiceNinja).

5. **r/webdev** — short Showoff Saturday version + an answer-where-asked one-liner for relevant threads.

6. **r/indiehackers** — founder build story framing: the constraint (100% client-side), the implementation detail (browser print-to-PDF), the features that grew out of that constraint, the stack (vanilla JS, GitHub Pages).

**Also included:** AlternativeTo angle (list as free alternative to Invoice Simple, Zoho Invoice, Wave Invoicing), SaaSHub submission guidance, and a template for ongoing answer-where-asked engagement in r/freelance, r/freelancers, r/digitalnomad, r/smallbusiness.

**Posting order note:** Product Hunt → r/SideProject → r/freelance → Show HN → r/webdev (Showoff Saturday) → r/indiehackers. PH best on Tuesday/Wednesday 12:01 AM PT; HN weekdays 9–11 AM ET.

### Why this batch

Task (d) was the top candidate in the 2026-07-13 entry and had been explicitly listed as deferred "multiple consecutive runs." InvoiceQuick is the right tool to launch first for three reasons: (1) its audience (freelancers, contractors, small-business owners) is the widest in the suite and overlaps least with the existing developer-focused guide traffic; (2) two solid guides make the tool credible to show — the create-PDF and send-email guides give context to any product-page reviewer who clicks through; (3) the "100% in-browser, no signup, no watermark" value proposition is genuinely differentiated and easy to explain in a single sentence.

The privacy angle (your client names, rates, and billing data never leave your browser) is the primary differentiator vs. every named alternative and the most likely reason a privacy-conscious freelancer would switch from their current tool.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden. All posts in `launch-invoicequick.md` are for the owner to fire from their own accounts.

### Next candidates

- **LeakCheck → third guide: "how to set up pre-commit secret scanning"** — pre-commit/git-hook workflow automation; deeper toolchain angle than the existing two LeakCheck guides (incident fix + history scan); targets the team/senior-dev audience who wants to prevent commits, not just detect them after.
- **DepCheck → third guide: "what is npm typosquatting and how to detect it"** — DepCheck's key differentiator vs. npm audit; neither existing DepCheck guide focuses on typosquatting specifically; the attack vector is concrete and shareable.
- **Task (d) rotation → ShipSafe or HardenCheck launch posts** — ShipSafe has three guides (the most of any tool); HardenCheck has four. Either would be a credible next launch kit.

---

## 2026-07-13

**Batch: SEO content (priority a) — ShipSafe third guide: ADA compliance checklist for web developers**

### What shipped

**New page:** `shipsafe/guides/ada-compliance-checklist.html`
Live URL: https://labs.copperbaytech.com/shipsafe/guides/ada-compliance-checklist.html
Git commit: `c409ed8` pushed to `dukotah/shipsafe` main

A 30-item WCAG 2.1 AA checklist guide targeting the queries **"ADA compliance checklist for web developers"**, **"WCAG checklist"**, **"ADA website compliance checklist"**, and **"how to make a website ADA compliant checklist"** — a developer who already knows compliance is required and wants a systematic, testable reference to work through, not another explanation of why it matters. Different format (checklist) and different search intent (build/audit stage) from the two existing ShipSafe guides (WCAG 2.2 changes, AI site gaps).

**Article structure (all content is genuinely useful, WCAG-accurate):**

30 items organized by POUR principle, each with a pass condition, test method, and fix note:

- **Perceivable (8):** alt text (demand-letter tag), captions, audio transcript, 4.5:1/3:1 contrast with large-text definition (demand-letter tag), UI component contrast 3:1, color-not-sole-indicator (greyscale DevTools test), 200% resize, no images of text
- **Operable (9):** keyboard-only functionality, no keyboard trap, skip nav (demand-letter tag), unique page title, logical focus order (positive tabindex antipattern), visible focus indicator (demand-letter tag), no 3×/sec flash, time limits adjustable, target size 24×24px (WCAG 2.2 badge)
- **Understandable (8):** lang attribute, form labels (demand-letter tag; placeholder ≠ label), field-specific error messages, required fields declared, descriptive headings no level skip, descriptive link text, consistent nav position, no unexpected context change on input
- **Robust (6):** autocomplete on personal-data inputs, custom component name/role/state, no aria-hidden on focusable parent, status messages via aria-live, landmark elements and table scope, focus not hidden behind sticky headers (WCAG 2.2 badge)

Quick-wins section: 5 prioritized fixes for developers with limited time, ordered by demand-letter litigation frequency. FAQPage JSON-LD schema (6 questions): most common demand letter criteria; WCAG 2.1 vs 2.2 coverage; professional vs. self-audit; automated tools; audit time estimate; screen reader testing.

**Also updated:**
- `shipsafe/sitemap.xml` — added new guide URL at priority 0.8, lastmod 2026-07-13
- `shipsafe/index.html` — added "ADA checklist" to header nav (after "WCAG 2.2") and footer nav
- `shipsafe/guides/wcag-compliance-ai-generated-sites.html` — added ADA checklist footer link
- `shipsafe/guides/wcag-2-1-vs-2-2-what-changed.html` — added ADA checklist footer link; bidirectional cross-link established across all three ShipSafe guides

### Why this batch

ShipSafe had two guides covering narrow sub-audiences (AI-site developers; WCAG 2.1→2.2 upgraders). The ADA checklist fills a larger, evergreen search query: the developer at the BUILD or AUDIT stage who wants a systematic, testable reference. "ADA compliance checklist" is a head query generating sustained search volume because it recurs every time a developer starts a new project or gets an accessibility complaint. Key differentiators: test methods on every item (not just what to pass, but how to verify), demand-letter litigation-risk tags on the top 5 items, WCAG 2.2 items badged distinctly so developers understand the upgrade path, and the greyscale emulation test method for the color-as-information criterion.

The three ShipSafe guides now cover three distinct, non-overlapping search moments: discovery/awareness (AI sites) → upgrade path (WCAG 2.2 changes) → build/audit stage (this checklist).

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next guide candidates

- Task (d) — InvoiceQuick launch posts (broadest non-developer audience in suite; has 2 guides; no `launch-invoicequick.md` exists; deferred multiple consecutive runs)
- DepCheck → third guide: "what is npm typosquatting and how to detect it" (typosquatting is DepCheck's key differentiator vs. npm audit; neither existing DepCheck guide covers this angle)
- LeakCheck → "pre-commit secret scanning setup guide" (workflow-automation focus; more toolchain-depth than the existing git-repo scanning guide)

---

## 2026-07-11

**Batch: SEO content (priority a) — ExposureCheck second guide: how to check if a website has exposed files**

### What shipped

**New page:** `exposurecheck/guides/how-to-check-website-exposed-files.html`
Live URL: https://labs.copperbaytech.com/exposurecheck/guides/how-to-check-website-exposed-files.html
Git commit: `6d64561` pushed to `dukotah/exposurecheck` main

A ~1,000-word detection-intent audit guide targeting the queries **"how to check if a website has exposed files"**, **"website exposed files check"**, **"check if site has public .git folder"**, and **"how to detect exposed .env on website"** — a developer, security professional, or bug-bounty researcher who wants to proactively audit a site before launch or on a routine basis. This is a distinct search moment from the existing fix guide (which is fix-intent, post-incident): this guide targets people who haven't confirmed a problem yet and want to run a thorough proactive audit.

**Article structure (all content is genuine, technically accurate):**
- Legal scope callout: only test sites you own; CFAA/CMA references
- The 6 most commonly exposed paths, each with severity rating (Critical/High) and explanation:
  1. `.env` and variants (bots probe within minutes; most impactful single exposure)
  2. `.git/config` (full repo reconstruction possible via GitTools; exposes private repos, commit history, embedded auth tokens)
  3. JavaScript source maps `*.js.map` (reveals full original source code including comments, types, file structure, dev-time hardcoded values)
  4. `config.json` / `config.yml` (framework-specific predictable locations)
  5. Database backup files `backup.sql`, `dump.sql` (full data copy, PII, hashed passwords)
  6. `wp-config.php` / `phpinfo.php` (WordPress credentials, PHP environment info)
- Manual curl-based audit: single-command status probe; HTTP response code interpretation table (200=danger, 301/302=check, 403=safe, 404=safe); "check that 200 actually returns content" tip
- Complete shell audit loop: one-liner that probes all high-risk paths and reports each HTTP status; pipe through `grep "^200"` to filter hits
- `.git` directory depth check: browseable directory listing, packed-refs access, HEAD branch confirmation
- Browser DevTools walkthrough: Sources/Debugger tab for source map detection; Network tab filter for `.map` requests; inline curl command for grepping bundle text for common key patterns (sk_live, sk_test, AKIA, ghp_, AIza)
- ExposureCheck CTA with coverage summary (path probing, bundle scanning, 40+ key patterns, severity grading, source map detection)
- Triage guide: what to do when each exposure type is found (incident-level vs. code-confidentiality vs. data-breach classifications)
- When to run audits: 5 specific triggers (launch, platform change, pipeline change, new service, quarterly baseline)
- FAQ + FAQPage JSON-LD schema (6 questions): fastest .env check; .git folder detection; source map contents; authorization scope; audit frequency; ExposureCheck coverage limits

**Also updated:**
- `exposurecheck/sitemap.xml` — added new guide URL at priority 0.8, lastmod 2026-07-11; added lastmod to existing fix-guide entry
- `exposurecheck/index.html` — split header nav into "Audit guide" and "Fix guides" (was a single "Guides" link)
- `exposurecheck/guides/fix-exposed-env-file.html` — added "Audit guide" to header nav; added "How to audit for exposures" to footer nav; added inline cross-link in Prevention section; bidirectional cross-link established

### Why this batch

ExposureCheck had only one guide (fix-exposed-env-file) targeting the post-incident panic query. The audit guide fills a different, adjacent search moment: the proactive developer or security professional who wants to know *how to check* before anything has gone wrong. These two audiences search differently and land at different moments in their mental state — one is in triage, one is being methodical. The audit guide also serves bug bounty researchers and penetration testers who legitimately probe their own infrastructure, a distinct high-intent segment that the fix guide doesn't reach.

Key differentiators vs. generic "website security audit" articles:
1. The complete shell audit loop — a single copy-paste command that probes all critical paths — is the primary shareable artifact; most articles explain what to check without giving the actual commands
2. The `.git` directory depth-check section (browseable listing → packed-refs → HEAD confirmation) covers the full reconstruction risk, not just "check if /.git/config returns 200"
3. The triage guide (incident-level vs. code-confidentiality vs. data-breach classification per exposure type) is the decision guide developers need after finding something — it prevents both under-reaction (dismissing a source map as low-risk) and over-reaction (treating a non-credential disclosure as an active incident)
4. The authorization callout at the top (CFAA/CMA) protects the tool legally and positions ExposureCheck correctly for self-service defensive use

The two guides now form an interlocking pair: audit-to-find → fix-what-you-found. Both cross-link to each other, creating a bidirectional internal link structure that strengthens the authority of both pages and matches the natural user journey.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next guide candidates

- LeakCheck → "how to scan for secrets before deploying to production" (pre-commit prevention angle; third LeakCheck guide; targets developers who want to prevent incidents rather than fix them; pairs with the existing accidentally-committed guide)
- Task (d) — InvoiceQuick launch posts (no per-tool launch-*.md exists yet; InvoiceQuick now has 2 guides which makes it more credible to showcase; broadest non-developer audience in the suite)
- ShipSafe → third guide: "ADA compliance checklist for web developers" (compliance-checklist format; high-intent developers who passed an audit and want a systematic list to verify; different from both the AI sites guide and the WCAG 2.1 vs 2.2 guide)

---

## 2026-07-09

**Batch: SEO content (priority a) — ComplyKit second guide: what should a privacy policy include**

### What shipped

**New page:** `complykit/guides/what-should-a-privacy-policy-include.html`
Live URL: https://labs.copperbaytech.com/complykit/guides/what-should-a-privacy-policy-include.html
Git commit: `15f9c46` pushed to `dukotah/complykit` main

A ~900-word compliance-checklist guide targeting the queries **"what should a privacy policy include"**, **"privacy policy checklist"**, **"what to include in a privacy policy"**, and **"privacy policy required sections"** — a builder who has already decided they need a privacy policy (or been told they do) and wants to know what it must actually say, section by section.

**Article structure (all content is genuine, legally accurate):**
- Intro: why a mismatch between policy and actual practices is worse than having no policy
- The 9 required sections, each as a named card with what it must say and a "Required by" attribution:
  1. What personal data you collect — specific categories, not vague buckets; implicit collection (analytics, server logs) is the most common gap
  2. Purpose and legal basis — GDPR's six lawful bases; why "to improve your experience" is not a purpose
  3. Third-party processors — named tools (Google Analytics, Stripe, Mailchimp, hosting providers); CPRA "sharing" disclosure for ad networks
  4. Retention periods — the most commonly missing section; GDPR Art. 13(2)(a) requires specific periods or criteria; "as long as necessary" doesn't satisfy it
  5. User rights — GDPR rights (access, rectification, erasure, portability, objection) vs. CCPA rights (know, delete, correct, opt-out)
  6. Cookie disclosure — ePrivacy Directive consent vs. notice-only banners; link to cookie notice
  7. Contact information — data controller identity; DPO exemption for most small sites
  8. Children's data statement — COPPA/GDPR Art. 8; minimum required statement for general-audience sites
  9. Last updated date — CalOPPA explicit requirement; update whenever practices change
- Per-law coverage table (9 sections × 4 frameworks: GDPR, CCPA/CPRA, CalOPPA, COPPA)
- 5 common mistakes that create liability: copying templates, omitting implicit collection, vague purpose language, never updating post-launch, burying the link
- ComplyKit CTA (generates all 9 sections from questionnaire, entirely in-browser)
- Cross-link callout to first guide (do-i-need-a-privacy-policy)
- FAQ + FAQPage JSON-LD schema (6 questions): lawyer requirement; most commonly missing section; copying templates; legal basis for processing; Do Not Sell link; update frequency

**Also updated:**
- `complykit/sitemap.xml` — added new guide URL at priority 0.8, lastmod 2026-07-09; updated first guide lastmod to 2026-07-09
- `complykit/index.html` — updated header nav ("Fix guides" split into "Do I need a policy?" and "What to include"); updated footer nav with both guides individually
- `complykit/guides/do-i-need-a-privacy-policy.html` — added "What to include" link in header nav and footer nav; added inline cross-link in the "What your policy must say" section; bidirectional cross-link established

### Why this batch

ComplyKit's first guide ("do I need a privacy policy") covered the threshold question: does the law require one? This second guide addresses the next question in the user journey: what does it actually need to say? These are two distinct search moments. The person who finds the first guide has confirmed they need a policy and now wants to know the contents — but that's a different query with different intent. The per-law coverage table is the primary differentiator vs. generic "privacy policy checklist" articles: it shows which of the 9 sections comes from which law, which makes it immediately useful to a developer who is EU-focused, US-focused, or needs to satisfy both at once. The retention periods section is the second differentiator — it's the most commonly absent section in boilerplate policies and the one most frequently cited by GDPR enforcement actions against small sites.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next guide candidates

- ExposureCheck → "how to check if a website has exposed files" (second ExposureCheck guide; detection-intent vs. fix-intent; targets proactive auditors, security teams, and curious developers; different from the existing fix guide)
- Task (d) — InvoiceQuick launch posts (broadest non-developer audience in the suite; now has 2 guides which makes it more credible to showcase; no per-tool launch-*.md files exist yet)
- LeakCheck → second guide: "how to scan for secrets before deploying to production" (pre-commit prevention angle vs. the existing post-commit remediation guide)

---

## 2026-07-07

**Batch: SEO content (priority a) — InvoiceQuick second guide: how to send a professional invoice by email**

### What shipped

**New page:** `invoicequick/guides/send-invoice-email.html`
Live URL: https://labs.copperbaytech.com/invoicequick/guides/send-invoice-email.html
Git commit: `3009823` pushed to `dukotah/invoicequick` main

A ~950-word send-and-collect guide targeting the queries **"how to send a professional invoice by email"**, **"invoice email template"**, **"freelance invoice email"**, and **"how to follow up on an unpaid invoice"** — a freelancer or small-business owner who has generated their invoice and now needs to know how to send it, name it correctly, and chase payment professionally. This is a distinct search moment from the first guide (create/export) and captures the post-creation segment of the same audience.

**Article structure (all content is genuine, accurate, globally applicable):**
- Subject line formula — `Invoice [number] – [Business name] – Due [date]`; why vague subjects ("Invoice attached") get ignored in AP departments; recurring-invoice variant with billing period; why the subject serves both routing and searchability simultaneously
- Email body template — 5-line cover note; what to include vs. what to cut (project backstory belongs elsewhere; apologies for sending the invoice signal professional uncertainty); tip callout on repeating the payment method in the email body
- Naming the PDF attachment — `INV-[number]-[ClientShortName]-[YYYY-MM].pdf` convention; why `invoice.pdf` creates filing problems for the client and recall problems for you
- Three-point pre-send checklist — payment details on the PDF itself; confirmed AP email address (not project contact); invoice number/amount/due date matches what was agreed
- Vendor portals section — SAP Ariba, Coupa, Bill.com, Tipalti, Basware; why email invoices disappear at enterprise clients using these systems; the tell-tale signs (vendor number, supplier registration, PO references); ask before the first invoice callout
- Three-tier follow-up cadence with copyable email templates: Tier 1 (due date — warm reminder, re-attach invoice), Tier 2 (7 days overdue — direct, no apologies, offer to resend payment details), Tier 3 (14–21 days — formal demand, UK/EU Late Payment Act and US interest clause reference, formal-recovery warning)
- When a client goes silent — internal escalation strategy; solicitor's letter (UK pattern, ~£50–100, high success rate); small claims (Money Claim Online UK; state-by-state US limits); collections agencies with percentage caveat
- Record-keeping — BCC self practice; date-stamped audit trail; jurisdiction retention requirements (UK/EU 6yr, US 7yr, AU 5yr); folder structure tip callout
- FAQ + FAQPage JSON-LD schema (6 questions): business email address; finding the right AP address; PDF file size concerns; WhatsApp/Slack delivery risks; "I never received it" handling; email acknowledgement as legal evidence

**Also updated:**
- `invoicequick/sitemap.xml` — added new guide URL at priority 0.8, lastmod 2026-07-07; bumped index and first guide lastmod to 2026-07-07
- `invoicequick/index.html` — updated footer nav to list both guides individually ("How to create an invoice PDF" / "How to send an invoice"); updated FAQ "How do I get a PDF?" answer to cross-link both guides
- `invoicequick/guides/create-free-invoice-pdf.html` — added send-invoice link to header nav and footer nav; bidirectional cross-link established

### Why this batch

InvoiceQuick had only one guide after the first-cycle rotation (the create-PDF guide). The send guide targets a different and equally high-intent search moment: a freelancer who has the PDF in hand and doesn't know the right subject line, doesn't know to ask about vendor portals, and doesn't have a professional follow-up template. The vendor portal section is the primary differentiator vs. generic invoice-email articles — almost none cover the "your email invoice will be ignored by any enterprise client using Ariba or Coupa" scenario, which is a real and frustrating surprise for freelancers billing enterprise clients for the first time. The three-tier follow-up cadence with actual copyable email templates (not just describing what to write) is the second differentiator.

The two guides now form an interlocking pair matching the full invoice lifecycle: create → send → follow up → collect. Both cross-link to each other, creating a bidirectional internal link structure that strengthens the authority of both pages.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next guide candidates

- ComplyKit → "what should a privacy policy include" (second ComplyKit guide; targets the builder who has been told they need a policy and wants to know what it must actually say — different intent from the decision guide)
- ExposureCheck → "how to check if a website has exposed files" (second ExposureCheck guide; detection-intent vs. fix-intent; targets proactive auditors and security teams)
- Task (d) rotation — launch posts for InvoiceQuick (broadest non-developer audience in the suite; now has 2 guides which makes the tool more credible to showcase)

---

## 2026-07-05

**Batch: SEO content (priority a) — HardenCheck fourth guide: X-Content-Type-Options nosniff**

### What shipped

**New page:** `hardencheck/guides/x-content-type-options-nosniff.html`
Live URL: https://labs.copperbaytech.com/hardencheck/guides/x-content-type-options-nosniff.html
Git commit: `7eee09a` pushed to `dukotah/hardencheck` main

A ~850-word guide targeting the queries **"X-Content-Type-Options nosniff"**, **"what is nosniff header"**, **"add X-Content-Type-Options header"**, and **"MIME type sniffing attack"** — a developer whose security scanner flagged a missing or unset `X-Content-Type-Options` header and who wants to understand what the header actually prevents before adding it.

**Article structure (all content is genuine, technically accurate):**
- What MIME sniffing is — how browsers invented content-type guessing for misconfigured servers; the gap between what the server declares and what the browser executes
- How sniffing becomes an attack — the GIF-wrapped script pattern; file upload endpoints; attacker uploads a file with a GIF magic header + `<script>` body; browser sniffs it as HTML/JS and executes in origin context; cookie theft example
- What `nosniff` actually blocks (and what it doesn't) — two-row table: scripts (refuses execution if Content-Type is not a JS MIME type) and stylesheets (refuses parsing if Content-Type is not text/css); images/audio/video are unaffected; key callout: nosniff and CSP are complementary, not redundant
- Why it still matters in 2026 — four specific reasons: CDN/object store MIME misconfiguration, CSP `script-src 'self'` gap (same-origin MIME confusion still executes under a self-only CSP), browser variance by context and resource type, and it being a zero-cost header
- Check your server's MIME types before deploying — `curl -I yoursite.com/script.js` verification; tip callout for DevTools Network tab; what happens if you add nosniff with a misconfigured MIME type (scripts break by design)
- Per-platform deployment snippets: nginx, Apache (.htaccess), Vercel (vercel.json), Next.js (next.config.js), Netlify (_headers), Cloudflare Pages (_headers); GitHub Pages tip callout (no custom headers without Cloudflare proxy); combined header block for nginx showing all four core headers together
- Verify with HardenCheck CTA
- FAQ + FAQ JSON-LD schema (6 questions): does nosniff break anything; nosniff vs CSP; image-based attack scope; user file upload protection strategy; browser support history (IE8+); whether HardenCheck checks for it

**Also updated:**
- `hardencheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-07-05
- `hardencheck/index.html` — added "What is nosniff? →" link to the X-Content-Type-Options MIME feature card (matching the existing pattern from the CSP, HSTS, and XFO cards)

### Why this batch

Fourth guide in HardenCheck's rotation, targeting the last of the four main header flags that HardenCheck reports on. The MIME feature card in `index.html` was the only one of the four major header cards without a guide link; this closes that gap. "X-Content-Type-Options nosniff" is a common scanner output that developers copy-paste without understanding, which makes it a strong organic search target for the post-scan audience. The key differentiators vs. generic nosniff articles: (1) the GIF-wrapped script attack example with a concrete cookie-theft scenario makes the threat real rather than abstract; (2) the "CSP `script-src 'self'` does not prevent same-origin MIME confusion" section addresses the most common misconception (that a self-only CSP makes nosniff redundant); (3) the "check your MIME types first" section warns about the one real break risk before deployment, which other guides skip; (4) the combined nginx block at the end shows all four security headers together, which is the natural "while I'm here" context for a developer who just ran HardenCheck.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing Pro waitlist mailto on hardencheck/index.html untouched.

### Next guide candidates

- InvoiceQuick → "how to send a professional invoice by email" (second InvoiceQuick guide; targets the send/follow-up moment rather than the create/PDF moment; different search intent from the existing guide)
- Task (d) rotation — no per-tool `launch-<tool>.md` files exist yet (only suite-level); could prepare individual tool launch posts for InvoiceQuick or ComplyKit, both of which have the broadest non-developer audiences in the suite

---

## 2026-07-03

**Batch: SEO content (priority a) — ShipSafe second guide: WCAG 2.1 vs 2.2 what changed**

### What shipped

**New page:** `shipsafe/guides/wcag-2-1-vs-2-2-what-changed.html`
Live URL: https://labs.copperbaytech.com/shipsafe/guides/wcag-2-1-vs-2-2-what-changed.html
Git commit: `acbbe74` pushed to `dukotah/shipsafe` main

A ~950-word compliance-clarity guide targeting the queries **"WCAG 2.1 vs 2.2 what changed"**, **"do I need WCAG 2.2 compliance"**, **"WCAG 2.2 new success criteria"** — a developer who passed WCAG 2.1 AA and wants to know if they still meet the current standard, and what specific work 2.2 requires.

**Article structure (all content is genuine, technically and legally accurate):**
- The short answer: WCAG 2.2 published October 2023; 2.1 AA-passing sites need to check 5 new A/AA criteria; current US law (DOJ rules) still references 2.1 AA
- All 9 new SCs in a scrollable table with level (A/AA/AAA) and one-sentence description
- Detailed fix blocks for each of the 5 new Level A/AA criteria:
  - 2.4.11 Focus Not Obscured (AA) — sticky headers hiding focused elements; `scroll-margin-top` fix with real CSS
  - 2.5.8 Target Size Minimum (AA) — 24×24px rule; icon buttons; `padding` fix pattern with code
  - 2.5.7 Dragging Movements (AA) — carousels/sliders; who this affects and who it doesn't
  - 3.3.7 Redundant Entry (A) — multi-step form re-entry; password confirm exemption
  - 3.3.8 Accessible Authentication (AA) — reCAPTCHA v2 failure scenario; v3/passkeys/magic links as compliant alternatives
  - 3.2.6 Consistent Help (A) — help mechanism consistency; why most sites already pass
- SC 4.1.1 Parsing deprecation — what it means for sites that had 2.1 Parsing failures
- Legal reality section — DOJ April 2024 (Title II) and July 2024 (Title III) final rules both reference WCAG 2.1 AA; practical guidance on targeting 2.2 for new builds
- Tip callout: fastest path for 2.1 AA sites (check 3 things)
- Verify with ShipSafe CTA + cross-link to AI sites guide
- FAQ + FAQ JSON-LD schema (6 questions): law requires 2.1 or 2.2; passing 2.1 ≠ passing 2.2; 24×24 pixel rule; CAPTCHAs and 3.3.8; SC 4.1.1 Parsing deprecated; which changes matter for SaaS/marketing sites

**Also updated:**
- `shipsafe/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-07-03
- `shipsafe/index.html` — added "WCAG 2.2" link to header nav; added "WCAG 2.1 vs 2.2" link to footer nav
- `shipsafe/guides/wcag-compliance-ai-generated-sites.html` — added cross-link to new guide in footer nav

### Why this batch

Second guide in ShipSafe's rotation; this query has been listed as the next candidate since the 2026-06-27 entry. "WCAG 2.1 vs 2.2 what changed" is searched by a different audience than the first guide (AI site compliance): this is the developer who *already* knows about WCAG 2.1 and wants to understand the delta. The upgrade-path frame captures people immediately after any 2.1 audit — a high-intent moment that the existing guide doesn't address. Key differentiators vs. generic WCAG 2.2 summaries: (1) the legal reality section with the specific DOJ 2024 rule cites (most articles just say "check with a lawyer"); (2) the reCAPTCHA v2 failure scenario for 3.3.8 (developers running reCAPTCHA don't realize the image challenge variant may fail the criterion without a working audio alternative); (3) the `scroll-margin-top` CSS fix for 2.4.11 (everyone with a sticky header needs this); (4) the 4.1.1 Parsing deprecation note (developers with old Parsing audit failures need to know those don't carry over to 2.2 conformance).

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.

### Next guide candidates

- HardenCheck → "what is X-Content-Type-Options nosniff and why does it matter?" (fourth HardenCheck guide; next-most-flagged header after CSP, HSTS, and XFO; consistently the most frequent remaining scanner warning)
- InvoiceQuick → "how to send a professional invoice by email" (second InvoiceQuick guide; different intent from create-PDF; targets freelancers who want send/follow-up templates)
- Task (d) rotation — no per-tool `launch-<tool>.md` files exist yet (only suite-level); could prepare individual launch posts for InvoiceQuick or ComplyKit

---

## 2026-07-01

**Batch: SEO content (priority a) — HardenCheck third guide: X-Frame-Options vs CSP frame-ancestors**

### What shipped

**New page:** `hardencheck/guides/x-frame-options-vs-csp-frame-ancestors.html`
Live URL: https://labs.copperbaytech.com/hardencheck/guides/x-frame-options-vs-csp-frame-ancestors.html
Git commits: `0ea1b29` (guide), `dd36e37` (sitemap), `8a25447` (index.html) pushed to `dukotah/hardencheck` main

A ~950-word guide targeting the queries **"X-Frame-Options vs frame-ancestors"**, **"do I still need X-Frame-Options"**, **"X-Frame-Options deprecated"**, and **"CSP frame-ancestors clickjacking"** — a developer whose security scanner (HardenCheck or any other) flagged both a missing X-Frame-Options header and a CSP without a `frame-ancestors` directive, and who doesn't understand the relationship between them.

**Article structure (all content is genuine, technically accurate):**
- What the confusion is — scanners flag both XFO and frame-ancestors absence; they protect against the same attack (clickjacking), so the reader naturally wonders which they actually need to set
- What X-Frame-Options does — `DENY` / `SAMEORIGIN` / `ALLOW-FROM` table; key callout: `ALLOW-FROM` is dead (Chrome/Edge/Safari never supported it; Firefox dropped it in 2019); only two useful values remain
- What CSP `frame-ancestors` does — syntax for `'none'`, `'self'`, specific origins, wildcards; covers `<embed>` and `<object>` which XFO doesn't; callout: frame-ancestors only works in HTTP response headers, not in meta tags (a common misconception)
- Key differences table — 7 dimensions: browser support, block all, allow same-origin, allow third-party, wildcard support, embed/object coverage, meta tag delivery
- Precedence rules — what browsers actually do when both headers are present: modern browsers (Chrome/Firefox/Edge/Safari) use `frame-ancestors` and ignore XFO; IE ignores CSP entirely and uses XFO. Browser-by-browser precedence table.
- The practical recommendation — set BOTH; belt-and-suspenders; no conflict; covers IE 9+ through Chrome latest. Three scenario examples: no embedding, same-origin embedding, trusted partner embedding (with IE caveat)
- Tip callout: if you have no CSP yet, `X-Frame-Options: DENY` is a one-liner you can ship today while building out a full CSP
- Per-platform deployment snippets showing BOTH headers together: nginx, Apache (.htaccess), Vercel (vercel.json), Next.js (next.config.js), Netlify (_headers), Cloudflare Pages (_headers)
- GitHub Pages callout (no custom response headers on plain GitHub Pages; Cloudflare Transform Rule workaround)
- Verify with HardenCheck CTA — clarifies that HardenCheck reads both XFO and frame-ancestors inside CSP; passing CSP frame-ancestors is sufficient even without XFO
- FAQ + FAQ JSON-LD schema (6 questions): does XFO still work; ALLOW-FROM; what frame-ancestors covers that XFO doesn't; should you set both; does HardenCheck check both; does meta tag CSP cover frame-ancestors

**Also updated:**
- `hardencheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-07-01
- `hardencheck/index.html` — added "XFO vs frame-ancestors →" link to the X-Frame-Options feature card (matching the existing pattern from the CSP and HSTS cards)

### Why this batch

Third guide in HardenCheck's rotation. "X-Frame-Options vs frame-ancestors" is one of the most searched clarification queries for developers running security scanners — the confusion comes from two separate scanner warnings pointing at the same underlying threat. Most existing articles on this topic either explain each header in isolation or give a terse "use frame-ancestors" recommendation without explaining the precedence rules, the ALLOW-FROM graveyard, or the IE fallback rationale. This guide covers all three: the belt-and-suspenders recommendation (set both), the precise browser precedence table (who wins when both are present), and the practical `ALLOW-FROM` warning (it's dead; stop trying to use it). The meta tag callout for `frame-ancestors` is the most commonly missed gotcha — developers who can't set response headers often try a meta tag workaround, which silently fails. The GitHub Pages callout directly addresses the constraint many Copper Bay Labs users face (static sites on GitHub Pages).

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing Pro waitlist mailto on hardencheck/index.html untouched.

### Next guide candidates

- ShipSafe → "WCAG 2.1 vs 2.2: what changed and do I need to update my site?" (targets devs who passed a 2.1 scan and are now seeing 2.2 failures; strong second guide for ShipSafe)
- HardenCheck → "what is X-Content-Type-Options nosniff and why does it matter?" (fourth HardenCheck guide; next-most-flagged header after XFO)
- Task (d) rotation — no per-tool `launch-<tool>.md` files exist yet (only suite-level marketing docs); could prepare individual tool launch posts for InvoiceQuick or HardenCheck
