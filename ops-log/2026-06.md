# Copper Bay Labs — Ops Log — June 2026

## 2026-06-11

**Batch: SEO content (priority a) — ExposureCheck fix-intent remediation guide**

### What shipped

**New page:** `exposurecheck/guides/fix-exposed-env-file.html`
Live URL: https://labs.copperbaytech.com/exposurecheck/guides/fix-exposed-env-file.html

A 1,000-word step-by-step remediation guide targeting the fix-intent query **“how to fix exposed .env file”** — the highest-motivation searcher possible (they have an active incident and are looking for exactly what to do).

**Article structure (all content is genuine, actionable, and accurate):**
- How .env files end up publicly accessible (4 root causes)
- Step 1: Rotate credentials immediately — per-provider instructions for AWS, OpenAI/Anthropic, Stripe, GitHub tokens, Supabase, database URLs
- Step 2: Remove the file from deployment (static hosts + VPS)
- Step 3: Block at server level — platform-specific configs for Nginx, Apache (.htaccess), Netlify (_redirects), Vercel (vercel.json), Cloudflare Pages, GitHub Pages
- Step 4: Scrub git history — git-filter-repo (recommended) and BFG Repo-Cleaner, with force-push instructions and collaborator warning
- Step 5: Verify with ExposureCheck (internal CTA to tool)
- Prevention checklist (5 points)
- FAQ (5 questions: bot speed, whether to rotate if no one found it, CDN cache issues, DATABASE_URL rotation, git history misconception)

**Also updated:**
- `exposurecheck/sitemap.xml` — added guide URL at priority 0.8
- `exposurecheck/index.html` — added “Guides” link to header nav + footer nav; added guide link inside the “A secret showed up — what now?” FAQ answer

### Why this batch

Fix-intent queries (“how to fix X”) convert differently from awareness queries. Someone searching “how to fix exposed .env file” has already confirmed the problem exists, is in active remediation mode, and needs exactly what ExposureCheck does (verify the fix). The Verify CTA at the end of the article is a natural action the reader would want to take — no selling required.

No fix guides existed in any of the tool repos prior to this run.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked per guardrail: `contact@copperbaytech.com` email delivery unconfirmed in ledger. No new CTAs added. Existing CTAs untouched.
- **Hub repo** — DISTRIBUTION.md references a hub repo for cornerstone posts. That repo is not in this session’s scope. Guide deployed to the tool repo per session instructions (`exposurecheck/guides/`). Owner can add a hub cross-link manually or confirm hub repo access.

### Handoff for owner

1. Submit the new sitemap URL to Google Search Console:
   `https://labs.copperbaytech.com/exposurecheck/sitemap.xml`
2. Consider posting the Dev.to article draft from `marketing/devto-leakcheck.md` to complement the LeakCheck/ExposureCheck pair.

---

## 2026-06-13

**Batch: SEO content (priority a) — LeakCheck fix-intent remediation guide**

### What shipped

**New page:** `leakcheck/guides/accidentally-committed-api-key-github.html`
Live URL: https://labs.copperbaytech.com/leakcheck/guides/accidentally-committed-api-key-github.html

A ~1,000-word step-by-step remediation guide targeting the high-panic, high-intent fix query **“accidentally committed api key to github”** — someone who has just discovered a secret in a public commit and needs immediate action steps.

**Article structure (all content is genuine, actionable, and accurate):**
- How fast bots find leaked keys (GitHub Events API, <60-second harvesting, research backing)
- Step 1: Rotate/revoke immediately — per-provider instructions for AWS, OpenAI/Anthropic, Stripe, GitHub tokens, Supabase, Twilio/SendGrid, Google Cloud, database URLs with passwords
- Step 2: Audit what else leaked — LeakCheck CTA (scan the affected file/diff, 40+ patterns, browser-only)
- Step 3: Remove from codebase — replace hardcoded values with env-var references, .gitignore setup, `git rm --cached`
- Step 4: Scrub git history — git-filter-repo (recommended, Python) with both --path and --replace-text modes; BFG Repo-Cleaner (Java) on bare clone; force-push instructions and collaborator warning
- Step 5: Check GitHub Secret Scanning alerts — Security tab location, Partner Program auto-revocation caveat
- Prevention checklist (6 points: .gitignore, platform env vars, pre-commit scanning, minimal token scopes, rotation schedule, ExposureCheck post-deploy)
- FAQ + FAQ JSON-LD schema (6 questions: history vs. deletion, bot speed, private repos, GitHub auto-revoke, forks, making repo private as a fix)
- Suite cross-links: ExposureCheck (post-deploy scanning) and HardenCheck (HTTP hardening)

**Also updated:**
- `leakcheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-13
- `leakcheck/index.html` — added “Guides” link to header nav; added guide inline link inside FAQ “A key leaked — what now?” answer; added “Fix guides” footer nav link

**Git commit:** `b9a51b9` pushed to `dukotah/leakcheck` main

### Why this batch

Rotating target tool from ExposureCheck to LeakCheck to cover the portfolio. “Accidentally committed api key to github” is a very high-intent panic query — the user has a confirmed active problem and will follow every step in the article. LeakCheck is the natural pre-commit tool to promote at step 2 (audit what else is in the file), and the article creates a natural cross-link loop with ExposureCheck (post-deploy verification). The FAQ schema also targets the closely related question “does removing a file from GitHub delete it from history?” which ranks as a distinct query.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new CTAs added.
- **Task (d) — launch posts** — not done this run; rotating to keep SEO content momentum while the guide inventory is thin.

### Handoff for owner

1. Submit the updated LeakCheck sitemap to Google Search Console:
   `https://labs.copperbaytech.com/leakcheck/sitemap.xml`
2. Confirm `contact@copperbaytech.com` receives email (send it a test from your inbox). Until confirmed, task (c) — lead CTA rollout across depcheck/hardencheck/exposurecheck — remains blocked.
3. Next highest-value guide candidates (not yet written): DepCheck → “how to fix npm audit vulnerabilities”; HardenCheck → “how to add Content-Security-Policy header”; ShipSafe → “is my Lovable site ADA compliant”.

---

## 2026-06-15

**Batch: SEO content (priority a) — DepCheck fix-intent remediation guide**

### What shipped

**New page:** `depcheck/guides/fix-npm-audit-vulnerabilities.html`
Live URL: https://labs.copperbaytech.com/depcheck/guides/fix-npm-audit-vulnerabilities.html

A ~900-word step-by-step remediation guide targeting the fix-intent query **“how to fix npm audit vulnerabilities”** — a developer who has just run `npm audit` and is staring at a report of critical, high, and moderate findings across direct and transitive packages.

**Article structure (all content is genuine, actionable, and accurate):**
- Reading the output — severity table (critical/high/moderate/low), direct vs. transitive distinction, how to get JSON output
- Step 1: `npm audit fix` (the safe semver-compatible path) — commit the lockfile after
- Step 2: Manual targeted upgrades for findings that require a major-version jump — changelog review
- Step 3: Transitive dependency overrides — Option A: upgrade the parent package; Option B: the `overrides` field in package.json (npm 8.3+), with equivalents for Yarn (`resolutions`) and pnpm (`pnpm.overrides`) — plus a callout warning that overrides can break the parent
- Step 4: No fix available yet — check GitHub issues for timeline, assess reachability of the vulnerable code path, check for patched forks, document accepted risk
- Step 5: Replace abandoned packages — when the fix will never come; examples of commonly replaced deprecated packages; DepCheck CTA for finding unmaintained deps beyond CVEs
- Prevention in CI — `--audit-level` flag, committing lockfile, scheduled `npm outdated` reviews, DepCheck for non-CVE risk
- FAQ + FAQ JSON-LD schema (6 questions: why fix doesn't fix everything, --force safety, what transitive means, low/moderate severity calculus, no-fix strategy, audit-level in CI)
- Suite cross-links: LeakCheck, ExposureCheck, HardenCheck

**Also updated:**
- `depcheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-15
- `depcheck/index.html` — added “Fix guides” link to header nav and footer nav; updated “Found a vuln — now what?” FAQ answer to include inline link to the new guide

**Git commit:** `0900df7` pushed to `dukotah/depcheck` main

### Why this batch

Third in the fix-intent guide rotation (ExposureCheck → LeakCheck → DepCheck). “How to fix npm audit vulnerabilities” is one of the most searched developer queries — every Node.js project eventually runs `npm audit`, and the output is often opaque and intimidating. The three-tier structure (safe fix → manual upgrades → transitive overrides) covers the full spectrum of what a developer will encounter. The guide also occupies a gap that `npm audit`’s own docs and most blog posts don’t fill well: what to do with transitive vulnerabilities when the parent hasn’t released a patch. The DepCheck CTA is positioned at Step 5 (abandoned packages) where it’s most genuinely useful — `npm audit` doesn’t flag unmaintained deps at all.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new CTAs added. Existing CTAs (pro waitlist mailto on depcheck/index.html) untouched.

### Handoff for owner

1. Submit updated DepCheck sitemap to Google Search Console:
   `https://labs.copperbaytech.com/depcheck/sitemap.xml`
2. Confirm `contact@copperbaytech.com` receives email (send a test). Unblocks task (c) — lead CTA rollout across all tools.
3. Next guide candidates: HardenCheck → “how to add Content-Security-Policy header”; ShipSafe → “is my Lovable site ADA compliant / WCAG compliant”.

---

## 2026-06-17

**Batch: SEO content (priority a) — HardenCheck fix-intent guide: how to add Content-Security-Policy**

### What shipped

**New page:** `hardencheck/guides/add-content-security-policy-header.html`
Live URL: https://labs.copperbaytech.com/hardencheck/guides/add-content-security-policy-header.html

A ~950-word step-by-step guide targeting the fix-intent query **“how to add Content-Security-Policy header”** — a developer whose site just got flagged (by HardenCheck or any other scanner) for a missing or weak CSP, who needs to understand what to write and how to deploy it safely.

**Article structure (all content is genuine, technically accurate):**
- What CSP does and why it matters: XSS as #1 vuln class; CSP as the browser-level enforcement layer that limits what scripts run even after injection
- Start with report-only mode (CRITICAL advice most guides skip): `Content-Security-Policy-Report-Only` header, how to watch DevTools console for violations, iterate before enforcing
- Build a starter policy: all directives line-by-line with comments (`default-src`, `script-src`, `style-src`, `img-src`, `connect-src`, `font-src`, `frame-ancestors`); why `'unsafe-inline'` defeats XSS protection
- Eliminate `unsafe-inline` with nonces: what a nonce is, header + script tag pairing, Node/Express middleware example for per-request injection; hash alternative with `openssl` command for static sites
- Per-platform deployment snippets: nginx, Apache `.htaccess`, Vercel `vercel.json`, Next.js `next.config.js`, Netlify `_headers`, Cloudflare Pages `_headers` — all real, working syntax
- Common things that break: Google Fonts (fonts.googleapis.com + fonts.gstatic.com), GTM/GA (script-src + connect-src), CDN-hosted scripts, `data:` URIs for inline SVGs; tip callout about keeping allowlist tight
- Verify with HardenCheck CTA button
- FAQ (6 questions + FAQ JSON-LD schema): CSP vs Report-Only; CSP vs HTTPS; why unsafe-inline breaks XSS protection; allowing third-party analytics; whether CSP breaks sites; where to test before deploying
- Suite cross-links: LeakCheck, ExposureCheck, DepCheck, ShipSafe

**Also updated:**
- `hardencheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-17
- `hardencheck/index.html` — added “Fix guides” link to header nav + footer nav; added inline guide link in CSP feature card (“How to add one →”)

**Git commit:** `40c9f91` pushed to `dukotah/hardencheck` main

### Why this batch

Fourth in the fix-intent guide rotation. HardenCheck is the natural home for this query — the tool flags CSP absence and the guide closes the loop by telling the developer exactly what to add. “How to add Content-Security-Policy header” is a high-volume fix-intent query from developers who just ran any security scanner on their site. The report-only section is the key differentiator: most articles tell developers to copy-paste a strict policy; this one explains why that breaks things and how to iterate safely. The nonce/hash section is the practical step most devs are stuck on (they know `unsafe-inline` is bad but don’t know the alternative).

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new CTAs added. Existing CTAs on hardencheck/index.html (Pro waitlist mailto) untouched.
- **Gmail send** — Gmail MCP requires OAuth; owner has not completed the one-time browser auth. Email written to `ops-log/email-2026-06-17-ops.md` as fallback.

### Next guide candidates

- ShipSafe → “is my Lovable site ADA compliant / WCAG compliant” (ShipSafe’s most relevant fix-intent query)
- ComplyKit → “do I need a privacy policy for my website” (very high search volume, strong intent alignment)

---

## 2026-06-19

**Batch: SEO content (priority a) — ShipSafe fix-intent guide: WCAG compliance for AI-generated sites**

### What shipped

**New page:** `shipsafe/guides/wcag-compliance-ai-generated-sites.html`
Live URL: https://labs.copperbaytech.com/shipsafe/guides/wcag-compliance-ai-generated-sites.html
Git commit: `4e9cb38` pushed to `dukotah/shipsafe` main

A ~950-word step-by-step guide targeting the fix-intent query **“WCAG compliance for AI-generated sites”** and closely related queries (“how to make website ADA compliant”, “is my Lovable site accessible”) — a developer or builder who just got a ShipSafe report (or any other scan) showing their site has accessibility failures and needs to know exactly what to fix.

**Article structure (all content is genuine, technically accurate):**
- Why AI builders ship accessibility gaps by default — the optimization-for-visual-output explanation; why WCAG failures are invisible in the browser but detectable by scanners
- Fix 1: Add `lang` attribute to `<html>` tag — WCAG 3.1.1 Level A — with per-platform instructions (Lovable code view, Webflow Site Settings, WordPress language setting)
- Fix 2: Label every form input — WCAG 4.1.2 Level A — `for`/`id` pairing + `aria-label` fallback for design-constrained layouts; why placeholder text is not a label
- Fix 3: Add alt text to images — WCAG 1.1.1 Level A — informative vs. decorative distinction, what “wrong” alt text looks like (`alt="image"`)
- Fix 4: Fix color contrast — WCAG 1.4.3 Level AA — 4.5:1 ratio requirement, how to check with Chrome DevTools and WebAIM Contrast Checker, common failure example (#999 on white = 2.85:1)
- Fix 5: Add a skip-to-main-content link — WCAG 2.4.1 Level A — complete HTML + CSS snippet, visually-hidden-but-focus-visible pattern
- Tip callout linking to ComplyKit for privacy policy (CCPA claims often bundled in ADA demand letters targeting California)
- Verify with ShipSafe CTA button
- FAQ + FAQ JSON-LD schema (6 questions: AI site risk reality, WCAG level to target, small business/side project liability, ADA vs WCAG distinction, accessibility/SEO overlap, how to document compliance)
- Suite cross-links: ComplyKit, LeakCheck, HardenCheck, ExposureCheck

**Also updated:**
- `shipsafe/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-19
- `shipsafe/index.html` — added “Fix guides” link to header nav + footer nav; added inline guide link in FAQ “Is my AI-generated site really at risk?” answer

### Why this batch

Fifth in the fix-intent guide rotation (ExposureCheck → LeakCheck → DepCheck → HardenCheck → ShipSafe). “Is my Lovable site ADA compliant” and “WCAG compliance AI-generated sites” are the highest-intent queries for ShipSafe’s exact niche — someone whose AI site was just flagged and needs to know what to change. The guide closes the loop perfectly: ShipSafe finds the problem, the guide explains how to fix it. The per-platform fix notes for Lovable, Webflow, and WordPress are a differentiator vs. generic WCAG guides that assume server access. The ComplyKit callout in the guide also creates a cross-tool link to the part of the suite with the lowest inbound traffic.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new CTAs added. Existing CTAs on shipsafe/index.html (Pro waitlist mailto) untouched.
- **Gmail send** — Gmail MCP requires OAuth browser flow; not authenticated in this session. Email written to `ops-log/email-2026-06-19.md` as fallback.

### Next guide candidates

- ComplyKit → “do I need a privacy policy for my website” (highest remaining unwritten guide; very high volume, strong intent alignment with ComplyKit’s exact function)
- InvoiceQuick → “how to create a free invoice PDF” (high-volume practical query, zero competition from the tool’s SEO footprint so far)

---

## 2026-06-21

**Batch: SEO content (priority a) — ComplyKit awareness/fix guide: do I need a privacy policy**

### What shipped

**New page:** `complykit/guides/do-i-need-a-privacy-policy.html`
Live URL: https://labs.copperbaytech.com/complykit/guides/do-i-need-a-privacy-policy.html
Git commits: `138569b` (guide + sitemap) + `2908e9f` (index.html nav update) pushed to `dukotah/complykit` main

A ~900-word awareness/decision guide targeting the high-volume query **“do I need a privacy policy for my website”** — a builder or site owner who is unsure whether the law requires one and, if so, what it must say. Converts directly to ComplyKit usage at the generate CTA.

**Article structure (all content is genuine, legally accurate):**
- Which laws require a privacy policy — table covering GDPR (EU/UK, no size threshold), CalOPPA (California, no size threshold, broad “commercial intent” interpretation), CCPA/CPRA (higher bar, but CalOPPA still catches most small sites), COPPA (children’s data), CAN-SPAM / ESP requirements
- What triggers the requirement — checklist of 7 common site features that constitute personal data collection: contact form, Google Analytics / any analytics, user accounts, newsletter signup, payments, live-chat widgets, server-side access logs. Key insight: an IP address alone is personal data under GDPR.
- What happens without one — GDPR fines up to €20M or 4% of revenue; CalOPPA AG enforcement; ADA demand letters (serial filers bundle CCPA missing-policy claim alongside accessibility allegations, same automated scan); Apple/Google app store rejection; Google Ads policy violation.
- What the policy must say — 7 required elements covering GDPR + CalOPPA/CCPA intersection: what data, purpose/legal basis, third-party processors, retention periods, user rights (access/delete/correct/opt-out), controller contact, last-updated date.
- Generate yours with ComplyKit — CTA explaining that ComplyKit generates all of this conditionally from the questionnaire, no boilerplate; disclaimer callout for complex situations.
- Tip callout for ShipSafe integration (ShipSafe flags missing privacy policy link; ComplyKit generates the policy; re-run ShipSafe to confirm cleared).
- FAQ + FAQ JSON-LD schema (6 questions): free sites with no sales; GDPR applies regardless of server location; what counts as personal data; consequences without a policy; validity of generated templates; where the link must appear on the site.
- Suite cross-links: ShipSafe, LeakCheck, HardenCheck, ExposureCheck.

**Also updated:**
- `complykit/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-21
- `complykit/index.html` — added “Fix guides” link to header nav and footer nav; added inline link to guide in the “Are the documents GDPR and CCPA compliant?” FAQ answer

### Why this batch

Sixth in the fix-intent/awareness guide rotation; ComplyKit was the last tool without a guide. “Do I need a privacy policy for my website” is one of the highest-volume compliance queries on the internet — it reaches exactly the person who is a single step away from using ComplyKit. Unlike the previous five guides (which are fix-intent for a confirmed problem), this one is decision-intent: the reader is asking a yes/no question and the answer is almost always “yes.” The guide answers the question fully, explains why (with a law table that covers the cases they don’t expect — GDPR applies to non-EU businesses; CalOPPA has no size floor), then makes generating the policy the obvious next action. The ShipSafe integration callout creates a bi-directional loop: ShipSafe sends flagged users to ComplyKit, and this guide sends ComplyKit users back to ShipSafe to confirm the fix is visible.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. Existing Pro waitlist mailto on complykit/index.html untouched; no new CTAs added.
- **Gmail send** — Gmail MCP draft tool available; email drafted below. If Gmail OAuth is active, draft will appear in Sent or Drafts.

### Next guide candidates

- InvoiceQuick → “how to create a free invoice PDF” (high-volume practical query; InvoiceQuick is the only tool in the suite without any guide content)
- Rotation complete for all 6 security/compliance tools. Next cycle could deepen: second guide per tool targeting a closely related query (e.g. ShipSafe → “WCAG 2.1 vs 2.2 differences”; DepCheck → “how to audit npm packages for security”).

---

## 2026-06-23

**Batch: SEO content (priority a) — InvoiceQuick how-to guide: how to create a free invoice PDF**

### What shipped

**New page:** `invoicequick/guides/create-free-invoice-pdf.html`
Live URL: https://labs.copperbaytech.com/invoicequick/guides/create-free-invoice-pdf.html
Git commit: `2eb4e42` pushed to `dukotah/invoicequick` main (via GitHub MCP; local git push returned 403)

A ~950-word how-to guide targeting the high-volume practical query **“how to create a free invoice PDF”** — a freelancer or small-business owner who needs to generate their first (or next) professional invoice and doesn’t want to sign up for another SaaS tool to do it.

**Article structure (all content is genuine, accurate, globally applicable):**
- What a professional invoice must include — 10-item checklist covering required fields from the word “Invoice” through to payment instructions; VAT/GST registration number note
- Invoice numbering — sequential best practice (INV-001, INV-002), year-prefixed and client-prefixed variants; why gaps and reused numbers are a problem with tax authorities in the UK, EU, and Australia
- Payment terms table — Due on receipt / NET 14 / NET 30 / 50% upfront; how to write payment instructions in the Notes field; UK Late Payment of Commercial Debts Act note for interest
- VAT, GST, sales tax table — UK/EU (VAT), Australia (GST), Canada (HST), New Zealand (GST), US (sales tax/nexus) — with registration thresholds and correct label for each; warning callout against charging tax while unregistered; EU B2B reverse charge explanation
- 6-step PDF export walkthrough — fill details → set invoice number + dates → add line items → set currency/tax/discount → add payment instructions in Notes → Download PDF + “Save as PDF” destination; A4 vs US Letter note; tip callout for .json reuse
- Sending the invoice — email template with informative subject line; enterprise vendor portal caveat (Ariba, Coupa, Bill.com); 3-step late-payment follow-up cadence
- InvoiceQuick CTA button
- FAQ + FAQ JSON-LD + HowTo JSON-LD schema (6 questions): legal validity; first invoice number; multi-currency; when to charge VAT/GST; monthly reuse; what to do if client doesn’t pay
- Suite cross-links: ComplyKit (privacy policy), ShipSafe (ADA risk), HardenCheck (security headers), LeakCheck (secret scanning)

**Also updated:**
- `invoicequick/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-23; bumped index lastmod to 2026-06-23
- `invoicequick/index.html` — added “Invoice guides” link to header nav and footer nav; added inline guide link in FAQ “How do I get a PDF?” answer

### Why this batch

Seventh and final in the first-pass guide rotation — InvoiceQuick was the only tool in the suite without any guide content. “How to create a free invoice PDF” is a high-volume, globally searched query from freelancers and small-business owners who need to send a professional invoice immediately. The guide is differentiated from generic invoice-format posts by: (1) the jurisdiction-by-jurisdiction VAT/GST table (most guides just say “check with your accountant”), (2) the reverse charge section for EU B2B, (3) the payment terms table comparing NET 14/30/due-on-receipt, and (4) the vendor portal caveat for enterprise clients. The InvoiceQuick CTA is positioned at step 6 of the how-to walkthrough, immediately after the reader understands exactly what to enter — the conversion path is zero friction.

The HowTo JSON-LD schema is a differentiator: Google surfaces HowTo rich results for this query class (visible steps in the SERP), which other generic “free invoice template” articles usually lack.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new mailto CTAs added. Existing Pro waitlist mailto on invoicequick/index.html untouched.

### Second-cycle guide candidates (first rotation now complete for all 7 tools)

- ShipSafe → “WCAG 2.1 vs 2.2: what changed and do I need to update my site?” (targeting developers who passed a 2.1 scan and are now seeing 2.2 failures)
- DepCheck → “how to audit npm packages for security before deploying” (broader query than the fix guide; targets proactive security posture rather than remediation)
- HardenCheck → “what is HSTS and how do I enable it?” (specific, high-intent, second-most-flagged header after CSP)
- LeakCheck → “how to scan for secrets in a git repository” (detection-intent rather than fix-intent; different audience segment)

---

## 2026-06-25

**Batch: SEO content (priority a) — HardenCheck second guide: how to enable the HSTS header**

### What shipped

**New page:** `hardencheck/guides/enable-hsts-header.html`
Live URL: https://labs.copperbaytech.com/hardencheck/guides/enable-hsts-header.html
Git commit: `c0e0309` pushed to `dukotah/hardencheck` main

A ~1,000-word step-by-step guide targeting the fix-intent queries **“how to enable HSTS”**, **“Strict-Transport-Security header”**, and **“what is HSTS and how do I add it”** — a developer whose site just got flagged by HardenCheck (or any security scanner) for a missing or weak HSTS header.

**Article structure (all content is genuine, technically accurate):**
- What HSTS does vs. what an HTTP→HTTPS redirect alone does — the SSL stripping attack (sslstrip tool) explained concretely; HSTS works at the browser level before any network packet leaves the device, so an attacker on the same network cannot intercept the first request
- Anatomy of the header — table of all three directives: `max-age` (required), `includeSubDomains` (optional but recommended), `preload` (optional, hard to undo)
- Step 1: Test with `max-age=300` first — 5-minute window; verification checklist (HTTPS on all resources/endpoints, no mixed-content warnings, all subdomains HTTPS); recovers quickly if something breaks
- Step 2: Raise `max-age` to 31,536,000 (1 year) — the value HardenCheck and most scanners require; note that the timer refreshes per-visit; callout table with common values (300/86400/2592000/31536000)
- Step 3: Decide about `includeSubDomains` — what it closes (cross-domain cookie attack via HTTP subdomain); subdomain audit checklist; the staging/preview URL caveat; Netlify/Vercel platform subdomain pattern to avoid the constraint
- Step 4: Understand preload before using it — preload list requirements (valid cert, HTTP redirect, max-age ≥ 31536000, includeSubDomains, preload); submission at hstspreload.org; hard warning: 3–6 month removal time, baked into browser binaries, no override
- Per-platform deployment snippets — nginx, Apache (.htaccess), Vercel (vercel.json), Next.js (next.config.js), Netlify (_headers), Cloudflare Pages (_headers); GitHub Pages tip callout (auto-sets HSTS, verify with paste mode)
- What HSTS doesn’t cover — cert quality, cipher suites, XSS/injection (CSP handles those); cross-link to CSP guide
- Verify with HardenCheck CTA button
- FAQ + FAQ JSON-LD schema (6 questions): TLS cert expiry during HSTS; removing HSTS with max-age=0 + preload removal caveat; MITM limitations; HSTS vs HTTPS distinction; includeSubDomains and staging subdomains; downside of very long max-age)
- Suite cross-links: CSP fix guide, LeakCheck, ExposureCheck, DepCheck, ShipSafe

**Also updated:**
- `hardencheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-25
- `hardencheck/index.html` — added “How to enable it →” link in the HSTS feature card (matching the existing pattern from the CSP card)

### Why this batch

First guide in HardenCheck’s second-cycle rotation. “How to enable HSTS” / “Strict-Transport-Security header” is the second-highest-intent query for HardenCheck’s niche — HSTS is the second-most-flagged header after CSP in any security scanner, and it appears in every HardenCheck report for sites not yet on HTTPS-enforced hosting. The key differentiators vs. generic HSTS guides: (1) the SSL stripping explanation makes the threat concrete — most guides say “you need HTTPS” without explaining why a redirect alone isn’t enough; (2) the four-step progression (300s → 1 year → includeSubDomains → preload) prevents the common mistake of deploying `max-age=31536000; includeSubDomains; preload` cold on a site that has HTTP-only staging subdomains; (3) the preload warning section is the most important content most HSTS guides bury or omit — the 3–6 month removal timeline and the “baked into browser binaries” consequence are not obvious to developers who think they can just remove the header.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new CTAs added. Existing Pro waitlist mailto on hardencheck/index.html untouched.
- **Gmail send** — No direct send tool available in this session (only create_draft). Email created as a Gmail draft — owner must open Gmail and send it.

### Next second-cycle guide candidates

- LeakCheck → “how to scan for secrets in a git repository” (detection-intent, different audience from the “accidentally committed” fix guide; targets proactive security teams)
- DepCheck → “how to audit npm packages for security before deploying” (proactive vs. remediation frame; complements the existing fix guide)
- HardenCheck → “what is X-Frame-Options and do I still need it with CSP frame-ancestors?” (third HardenCheck guide; clarifies a common confusion)

---

## 2026-06-27

**Batch: SEO content (priority a) — LeakCheck second guide: how to scan a git repository for secrets**

### What shipped

**New page:** `leakcheck/guides/scan-git-repository-for-secrets.html`
Live URL: https://labs.copperbaytech.com/leakcheck/guides/scan-git-repository-for-secrets.html
Git commit: `7864385` pushed to `dukotah/leakcheck` main

A ~950-word detection guide targeting the query **"how to scan for secrets in a git repository"** and closely related queries ("scan git history for secrets", "find api keys in git history", "gitleaks tutorial"). Different audience from the existing fix guide (incident response) — this targets the proactive/pre-deploy security posture: developers who want to audit what's already in their repo history, or set up prevention tooling.

**Article structure (all content is genuine, technically accurate):**
- Why history scanning matters — the common misconception that "deleting the file" removes the secret; the two distinct problems (secrets in history vs. secrets about to enter history)
- Tool comparison table mapping each use case to the right tool (LeakCheck browser scan, gitleaks detect, trufflehog, gitleaks protect hook, CI actions)
- Quick scan: LeakCheck browser paste — what to paste in (a .env file, git diff --cached output, CI configs, vendor config files)
- Full history audit with gitleaks — install (Homebrew, Linux binary, Scoop); `gitleaks detect --source . --log-opts="--all"` with JSON report flag; false-positive suppression via .gitleaks.toml allowlist
- Full history audit with trufflehog — install; `trufflehog git file://. --only-verified` vs `--no-verification`; key differentiator: live credential verification against provider APIs; warning about outbound calls and disclosure when scanning on behalf of others
- Pre-commit hook (two options): pre-commit framework with .pre-commit-config.yaml; raw git hook with `gitleaks protect --staged -v`
- GitHub Actions: gitleaks-action@v2 and trufflehog@main YAML; `fetch-depth: 0` callout (shallow clone is the most common misconfiguration that makes CI scans miss history)
- "Found a secret — now what?" section linking to the companion fix guide
- FAQ + FAQ JSON-LD schema (7 questions): gitleaks on private repos; detect vs protect distinction; scanning doesn't delete secrets; pre-commit hook scope; false positive suppression; what LeakCheck does that CLI tools miss; GitHub Secret Scanning vs own scanner

**Also updated:**
- `leakcheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-27
- `leakcheck/index.html` — added detection guide link in "Can I run this in CI?" FAQ answer; added "Detection guides" footer nav link

### Why this batch

Second guide in LeakCheck's rotation. "How to scan for secrets in a git repository" is high-intent but a different search moment from the existing fix guide ("I already leaked a key, help"). The detection-intent searcher is asking before an incident — typically after running an audit command, setting up a new project, or reading a security checklist. This expands LeakCheck's organic reach to that segment and creates a two-guide interlocking structure: if a scan finds something, the "Found a secret — now what?" CTA in this guide links to the fix guide; if someone lands on the fix guide, the "Scan before you commit" prevention section links back. The `fetch-depth: 0` section is a genuine differentiator — it's the most commonly missed configuration that makes GitHub Actions CI scans silently useless on existing history.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new CTAs added. Existing Pro waitlist mailto on leakcheck/index.html untouched.
- **Gmail send** — attempting via Gmail MCP; fallback email written to `ops-log/email-2026-06-27.md` if send fails.

### Next second-cycle guide candidates

- DepCheck → "how to audit npm packages for security before deploying" (proactive vs. remediation frame; different from the fix guide)
- HardenCheck → "what is X-Frame-Options and do I still need it with CSP frame-ancestors?" (third HardenCheck guide; common confusion)
- ShipSafe → "WCAG 2.1 vs 2.2: what changed and do I need to update my site?" (targets devs who passed a 2.1 scan but are now seeing 2.2 failures)

---

## 2026-06-29

**Batch: SEO content (priority a) — DepCheck second guide: how to audit npm packages for security before deploying**

### What shipped

**New page:** `depcheck/guides/audit-npm-packages-security.html`
Live URL: https://labs.copperbaytech.com/depcheck/guides/audit-npm-packages-security.html
Git commit: `7ca0104` pushed to `dukotah/depcheck` main

A ~900-word proactive pre-deploy guide targeting the query **"how to audit npm packages for security before deploying"** and closely related queries ("npm security audit before deploy", "audit npm packages for security", "npm dependency security check"). This is a different audience from the existing fix guide (which is reactive: you have findings, how do you fix them). The pre-deploy guide targets developers building the discipline of auditing *before* problems appear.

**Article structure (all content is genuine, technically accurate):**
- What npm audit misses — coverage comparison table with six risk categories: CVEs (npm audit covers), abandoned packages, deprecated packages, copyleft licenses, typosquatting, and zero-download packages (all four latter categories: npm audit does NOT cover; DepCheck does). This is the key differentiator framing.
- Step 1: Run npm audit locally before pushing — `npm audit`, `npm audit --json`, `--audit-level=high`; note on `npm ci` vs `npm install` in CI for lockfile consistency; link to the fix guide for remediation paths
- Step 2: Check for abandoned and deprecated packages — Option A: `npm info` per-package for spot checks; Option B: paste package.json into DepCheck for a full sweep. DepCheck CTA button here, where npm audit is genuinely insufficient.
- Step 3: License risk — GPL/AGPL/LGPL/EUPL copyleft explained with practical consequences; AGPL for web services highlighted as the most impactful; permissive-license safe list (MIT, BSD, Apache 2.0, ISC); warn-callout about transitive license exposure that npm info doesn't surface
- Step 4: Typosquatting — how attacks work, what to check before installing (npmjs.com download count, publisher account age, postinstall scripts); DepCheck's typosquatting heuristic
- Step 5: Wire into CI/CD — full GitHub Actions YAML example with `npm ci` + `npm audit --audit-level=high` + `schedule: cron` for weekly scans; pre-push git hook script for local enforcement; tip callout about why the cron trigger matters (catches advisories between deployments)
- Pre-deploy checklist — 7-item printable checklist with all the above categories
- FAQ + FAQ JSON-LD schema (7 questions): does npm audit catch everything; abandoned package risk; typosquatting definition; which licenses are risky; GitHub Actions CI setup; npm audit vs npm outdated; audit frequency

**Also updated:**
- `depcheck/sitemap.xml` — added guide URL at priority 0.8, lastmod 2026-06-29
- `depcheck/index.html` — added "Audit guide" link to header nav and footer nav; added inline guide link in "Is a clean scan a guarantee?" FAQ answer

### Why this batch

Second in DepCheck's second-cycle rotation. The existing fix guide covers the reactive case (you ran npm audit, you have findings, here's how to fix them). This guide covers the *proactive* case — what to run before you deploy, in the right order, to catch the categories of risk that npm audit doesn't surface. The coverage comparison table at the top is the genuine differentiator: most developers don't realize npm audit doesn't catch abandoned packages, license problems, or typosquatting. Framing DepCheck as the gap-filler for what npm audit misses (rather than a competitor to it) is more accurate and more persuasive. The `--audit-level=high` CI integration section + the weekly cron trigger tip fill a real knowledge gap — many teams run npm audit in CI but don't know to add the cron job, which means their 6-month-old deploy silently accumulates new advisories.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ledger. No new mailto CTAs added. Existing Pro waitlist mailto on depcheck/index.html untouched.
- **Gmail send** — Gmail draft created via MCP (draft ID: r-8854047727234930021). Open Gmail Drafts to review and send.

### Next second-cycle guide candidates

- HardenCheck → "what is X-Frame-Options and do I still need it with CSP frame-ancestors?" (third HardenCheck guide; clarifies confusion between legacy header and modern CSP directive; high search volume from devs running security scanners that flag both)
- ShipSafe → "WCAG 2.1 vs 2.2: what changed and do I need to update my site?" (targets devs who passed a 2.1 scan and are now seeing 2.2 failures)
