# Copper Bay Labs — Ops Log — August 2026

## 2026-08-01

**Batch: Task (d) — DepCheck launch posts**

### What shipped

**New file:** `forge/marketing/launch-depcheck.md`

One-paste-ready launch posts for DepCheck across seven channels (Show HN, Product Hunt, r/SideProject, r/webdev Showoff Saturday + answer-where-asked, r/netsec guide-only guidance, r/indiehackers) plus AlternativeTo/SaaSHub listing copy, timing notes, and a what-to-log section.

DepCheck was chosen for this run because it was the top task (d) candidate in the 2026-07-31 ops-log entry (and listed in the two prior entries before it) and is the only tool in the suite with three guides (fix CVEs, full pre-deploy audit, typosquatting deep-dive) and no launch kit. The typosquatting angle is the technically interesting differentiator for Show HN and r/netsec — npm audit has no category for typosquatted packages (they execute at install time, before a CVE is ever filed), and the existing guides cover this attack vector with enough depth to make the Show HN post and the r/netsec guide-share genuinely useful rather than just promotional.

**Contents of `launch-depcheck.md`:**

1. **Posting order** — Show HN is listed first (not Product Hunt), because the technically interesting angle (typosquatting + OSV in a single paste, no install required) is a better fit for HN's audience than PH's; posting HN first and referencing early feedback in the PH first comment is the better sequence for this tool.

2. **Show HN** — title, body, and first comment. The body is written in the technical-honest style HN rewards: explains the tool's approach (OSV lookup + edit-distance typosquatting + offline heuristics), acknowledges three limitations the owner should be aware of (heuristic-only typosquatting that misses numeral-for-letter substitutions, rate-limited OSV lookups for large manifests, the "abandoned vs stable" false-positive problem), and gives enough technical detail for HN to engage with. The first comment anticipates the four questions that will come up (vs npm audit, CI support, how typosquatting detection works, what data leaves the browser).

3. **Product Hunt** — tagline, full description (six check categories explained, privacy model, three guides), and a first comment as FAQ pre-empting the "isn't this just npm audit?" framing, what "typosquatting detected" actually means, offline capability, and CLI roadmap. PH topics: Developer Tools, Security, Open Source, Developer Experience.

4. **r/SideProject** — "I built this" framing. Leads with the real motivation (LLM-suggested packages accepted without scrutiny, npm audit's typosquatting blind spot), lists all six check categories, gives the privacy model. Invites specific feedback on the typosquatting heuristic.

5. **r/webdev** — Showoff Saturday post (title + body + feedback ask) and two answer-where-asked one-liners for pre-launch checklist threads and typosquatting/supply-chain threads specifically.

6. **r/netsec** — explicit "do NOT post the tool URL directly" guidance (mod policy), with two guide-share texts: the typosquatting guide for supply-chain discussion threads, and the audit guide for pre-deploy checklist threads. Both guide texts focus on the technical content of the guides rather than promoting the tool.

7. **r/indiehackers** — founder narrative: the gap in `npm audit` coverage (typosquatting, abandoned packages, licenses), the implementation trade-off I'd reconsider (edit-distance-only heuristic, OSV rate limits, the "abandoned vs stable" ambiguity), references to the three guides.

8. **AlternativeTo / SaaSHub** — alternatives to list as (Snyk, Socket.dev, npm audit, Dependabot), tags, and a description focused on the browser-only + no-install + six-check-dimensions angle.

9. **Timing notes** — Show HN before PH by 1–2 days, specific day/time guidance for each channel, r/netsec guidance on when to share guides vs staying silent.

10. **What-to-log section** — metrics to record back to metrics-ledger.md per channel: Show HN score cadence, PH finish position, inbound Pro waitlist sign-ups, r/indiehackers engagement on the typosquatting angle.

### Why DepCheck for task (d) this run

DepCheck was the top task (d) candidate in the most recent ops-log entry (2026-07-31) and had been listed in the two entries before it. Among the tools without a launch kit, it has the strongest Show HN angle: the combination of typosquatting detection + OSV lookups + no-install paste mode is technically interesting to a developer audience in a way that HardenCheck (another developer tool) and ShipSafe (which already has a kit) are not. The typosquatting guide — particularly the `npm pack` workflow for reading postinstall scripts before they execute — is the kind of genuinely useful defensive security content that HN and r/netsec actually engage with rather than downvote as promotion.

ComplyKit is the remaining tool without a launch kit (`launch-complykit.md` does not exist). It's the strongest next task (d) candidate: three guides exist (threshold decision, what to include, cookie consent requirements), and the compliance audience (developers and small-business owners making policy decisions) reaches channels the developer-focused tools don't fit as well (r/smallbusiness, r/legaladvice answer threads, r/indiehackers founders).

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing DepCheck Pro waitlist CTA untouched.
- **Auto-posting** — forbidden. All posts in `launch-depcheck.md` are for the owner to fire from their own accounts.

### Next candidates

- **ComplyKit → launch posts (task d)** — the last tool without a per-tool launch kit. Three guides exist (threshold, what to include, cookie consent). Compliance audience reaches channels the developer-focused tools don't fit (r/smallbusiness, r/legaladvice, r/indiehackers founders). Strong next task (d) candidate.
- **InvoiceQuick → third guide (task a)** — InvoiceQuick has only two guides while all security tools have three+. "How to invoice clients in another country" (international invoicing: currency, VAT/GST identification, payment rails) is a high-volume freelancer query with genuine global reach. Top task (a) candidate to close the two-guide gap.

---
