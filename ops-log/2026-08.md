# Copper Bay Labs — Ops Log — August 2026

## 2026-08-29

**Batch: Task (d) — LeakCheck launch posts**

### What shipped

**New file:** `forge/marketing/launch-leakcheck.md`

One-paste-ready launch posts for LeakCheck across all channels plus AlternativeTo/SaaSHub listing copy, answer-where-asked one-liners, timing notes, and a what-to-log section.

**Why LeakCheck / why now:**
The 2026-08-27 ops-log named "Task (d) — HardenCheck launch posts" as the top next candidate, but that file already exists at `forge/marketing/launch-hardencheck.md` (it was written before being captured in the ops-log). LeakCheck and ExposureCheck are the only two tools with no launch posts. LeakCheck was chosen first because it has 5 guides (the strongest content foundation of the two), and its tool has the highest-urgency use case in the suite — someone searching "accidentally committed API key" is in active crisis mode, which makes organic social posts more likely to land with a receptive audience.

**Channels prepared:**
1. **Show HN** — listed first (highest leverage for LeakCheck). The technical angles are genuinely interesting for HN: zero-network guarantee enforced in source code comments; named provider patterns vs. Shannon entropy heuristic for false-positive reduction; `first-4 + last-4` masking primitive; `textContent` (not `innerHTML`) for DOM safety. First comment prompts technical debate on false-positive rates and GitHub scraper timing.
2. **Product Hunt** — tagline, description, topic tags, first comment covering the four standard questions (privacy verification, comparison to CLI tools, pattern accuracy, git history limitation).
3. **r/SideProject** — founder-build frame, leads with the AI-coding credential leak problem.
4. **r/webdev** — Showoff Saturday post + answer-where-asked one-liner.
5. **r/indiehackers** — founder narrative on vibe-coding workflows and the credential leak failure mode.
6. **AlternativeTo / SaaSHub** — listing copy and tags (alternatives to GitGuardian, trufflehog, Snyk for the paste-and-check workflow).
7. **r/netsec note** — guidance on linking to guides rather than tool homepage (per r/netsec rules).

**Correction to 2026-08-27 ops-log:** That entry stated "LeakCheck, ExposureCheck, and HardenCheck have none [launch posts]." This was incorrect — `forge/marketing/launch-hardencheck.md` already existed. It had been created before the ops-log entry was written. The hardencheck kit covers 4 guides (the tool's launch-kit content was written before the 7-guide milestone); it should be updated to reference all 7 guides before the owner posts it.

### Blocked / not done
- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.
- **launch-hardencheck.md guide list update** — the existing hardencheck launch kit lists only 4 guides but HardenCheck now has 7. Worth updating before the owner posts it.

### Next candidates
- **Task (a) — ShipSafe sixth guide** — ShipSafe has 5 guides; all other tools now have 4+. A "WCAG audit checklist for existing websites" or "how to fix missing alt text across an entire site" guide targets high-volume remediation queries from site owners who received a scan result and need a systematic action plan. Named as next task (a) candidate in the 2026-08-27 entry.
- **Task (d) — ExposureCheck launch posts** — ExposureCheck now has 4 guides and is the last tool with no launch posts. After the ShipSafe guide, ExposureCheck launch posts are the next task (d) candidate.
- **Task (d) — launch-hardencheck.md update** — guide list update (4 → 7 guides, add Referrer-Policy, CSP for React/SPA, strict-dynamic guides).

---

## 2026-08-27

**Batch: SEO article (priority a) — InvoiceQuick fourth guide: how to write a freelance contract**

### What shipped

**New page:** `invoicequick/guides/freelance-contract-template.html`
Live URL: https://labs.copperbaytech.com/invoicequick/guides/freelance-contract-template.html
Git commit: `3c20ecd` pushed to `dukotah/invoicequick` main

**Target queries:** "freelance contract template", "how to write a freelance contract", "freelance service agreement", "independent contractor agreement", "freelance contract clauses", "freelance contract example", "freelancer service agreement template"

**Why this topic / why high-intent:**
InvoiceQuick had 3 guides (tied for fewest in the suite alongside ComplyKit after its 4th guide shipped on 2026-08-23). This guide was named explicitly as the top task (a) candidate in the 2026-08-23 and 2026-08-25 ops-log entries. The contract → invoice relationship is the natural adjacency: the contract sets the payment terms; the invoice implements them. A freelancer who found InvoiceQuick is exactly the person who also needs a contract — they are doing project-based work and being paid by invoice. The queries "freelance contract template" and "independent contractor agreement" are high-volume, high-intent searches from people who are about to start a project and need to formalise the engagement. The topic opens a new search cluster entirely distinct from the 3 existing InvoiceQuick guides (creating a PDF invoice, sending by email, international invoicing).

**Why distinct from the existing guides:**
The 3 existing guides all target the post-agreement billing workflow. This guide targets the pre-billing agreement stage — the moment before the client is even a client yet. It covers a categorically different pain point (scope creep, IP disputes, kill fees) rather than extending the invoicing cluster.

**Article structure (~950 words + example contract language):**

1. **Why a contract, not just an invoice** — scope disputes, revision loops, IP ambiguity, no kill fee, no late payment interest without written statement. The key framing: most freelance disputes are scope disputes, not payment disputes.
2. **9 essential clauses overview table** — parties, scope of work, timeline, payment terms, revisions, IP ownership, confidentiality, termination, governing law — with "what it prevents" and "key decision" columns.
3. **Scope of work deep-dive** — goals vs. deliverables; the importance of explicit exclusions.
4. **Payment terms with example language** — deposit structure, due date, late fee (1.5%/month), kill fee clause — all in copy-paste form.
5. **Revisions clause with example language** — round definition, 5-day feedback window, change order trigger.
6. **IP ownership** — the most important clause; work-for-hire vs. licensed use; ownership retains until payment in full (leverage); portfolio carve-out tip.
7. **Wiring the contract into InvoiceQuick invoices** — checklist: specific due date (not "net 14"), mirror late fee rate in Notes field, milestone labels, kill fee invoice language.
8. **InvoiceQuick CTA** — natural placement after the payment terms section.
9. **Minimum viable contract** — email chain for clients who resist formal agreements; offer + confirmation reply = binding in common-law jurisdictions.
10. **5 common mistakes** — goals vs. deliverables, no client-delay clause, skipping kill fee, using client's jurisdiction, starting without signature.
11. **Suite cross-links** — ComplyKit (GDPR privacy policy for freelance business site), ShipSafe, LeakCheck, HardenCheck.
12. **FAQ** — 7 questions with JSON-LD Article + FAQPage schema: do I need a lawyer; contract vs. SOW; does it need a signature; client refuses to sign; revision rounds; kill fee amount; late payment interest clause.

**Nav/sitemap also updated:**
- `invoicequick/sitemap.xml` — new guide URL added (lastmod: 2026-08-27)
- `invoicequick/index.html` — "Freelance contract" link added to header nav
- `invoicequick/guides/create-free-invoice-pdf.html` — header and footer nav updated
- `invoicequick/guides/send-invoice-email.html` — header and footer nav updated
- `invoicequick/guides/invoice-clients-international.html` — header and footer nav updated

InvoiceQuick now has **four guides** with consistent cross-navigation across all pages.

### Why this batch

InvoiceQuick had 3 guides — explicitly named the top task (a) candidate in the 2026-08-23 and 2026-08-25 ops-log entries. The freelance contract topic is categorically distinct from all 3 existing guides and opens a new high-volume search cluster (contract/agreement queries vs. invoicing/billing queries). The product connection is direct and honest: contract payment terms define what goes on the invoice.

### Blocked / not done
- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates
- **Task (d) — HardenCheck launch posts** — HardenCheck now has 7 guides (the strongest content foundation in the suite). `forge/marketing/launch-hardencheck.md` has not been written yet. DepCheck and ComplyKit launch posts are already prepared; LeakCheck, ExposureCheck, and HardenCheck have none. HardenCheck is the strongest next task (d) candidate.
- **Task (a) — ShipSafe sixth guide** — ShipSafe has 5 guides; all other tools now have 4+. A "WCAG audit checklist for existing websites" or "how to fix missing alt text at scale" guide targets a high-volume query from site owners who got a scan result but need to know how to act on it systematically.

---

## 2026-08-25

**Batch: SEO article (priority a) — HardenCheck seventh guide: how to set a Referrer-Policy header**

### What shipped

**New page:** `hardencheck/guides/referrer-policy-header.html`
Live URL: https://labs.copperbaytech.com/hardencheck/guides/referrer-policy-header.html
Git commit: `a0eaea9` pushed to `dukotah/hardencheck` main

**Target queries:** "referrer policy header", "referrer-policy strict-origin-when-cross-origin", "how to set referrer policy", "referrer policy best practice", "prevent url leakage referrer", "referrer header privacy"

**Why this topic / why high-intent:**
The Referrer-Policy header is flagged by HardenCheck (and every other security scanner) but has no guide in the suite. The queries "referrer policy header" and "referrer-policy strict-origin-when-cross-origin" are mid-to-high intent — developers who have received a scanner finding and need to know what to set and why. The topic is genuinely underserved: most existing content either lists the values without explaining the privacy implications, or only covers the browser-default change (which occurred in 2021–2022) without explaining why you still need to set it explicitly. This guide opens a new, non-overlapping search cluster from the existing six HardenCheck guides (which cover CSP, HSTS, X-Frame-Options, X-Content-Type-Options — all separate header families).

**Why distinct from enable-hsts-header.html:**
The ops-log 2026-08-23 entry named "HSTS preload requirements" as the 7th guide topic, but that guide already exists and covers preload in depth (`enable-hsts-header.html`). Repeating the topic would have been thin/duplicative. Referrer-Policy is a genuinely separate header that HardenCheck checks and that had no guide.

**Article structure (~950 words):**

1. **What the Referer header leaks** — the automatic header sent to external links, third-party resources, and API calls, with a five-row table of real-world URL leakage examples (reset tokens, user IDs, admin paths, promo codes, search queries).
2. **All eight policy values** — comparison table: value name, what same-origin sends, what cross-origin HTTPS sends, what cross-origin HTTP sends, and risk badge (Safe / Risky).
3. **Which value to use** — `strict-origin-when-cross-origin` recommendation with three-bullet explanation of why each behavior is correct; note on why setting it explicitly is necessary even though it's the browser default since 2021–2022.
4. **Platform deployment snippets** — nginx, Apache, Vercel, Next.js, Netlify, Cloudflare Pages, Express.js, and Helmet middleware.
5. **Per-page and per-element overrides** — `<meta name="referrer">` tag; `referrerpolicy` attribute on `<a>`, `<img>`, `<iframe>`.
6. **HardenCheck CTA**.
7. **Suite cross-links** — CSP guide, HSTS guide, X-Content-Type-Options guide, LeakCheck, ShipSafe.
8. **FAQ** — 7 questions with JSON-LD TechArticle + FAQPage schema.

**Nav/sitemap also updated:**
- `hardencheck/sitemap.xml` — new guide URL added (lastmod: 2026-08-25)
- `hardencheck/index.html` — "Referrer-Policy" link added to header nav; guide link added to the "REF" feature item in the "What we check" section; "Referrer-Policy guide" link added to footer nav
- All 6 existing guide pages (`add-content-security-policy-header.html`, `content-security-policy-strict-dynamic.html`, `csp-react-spa-app.html`, `enable-hsts-header.html`, `x-content-type-options-nosniff.html`, `x-frame-options-vs-csp-frame-ancestors.html`) — header nav and footer nav updated to include the new Referrer-Policy guide link

HardenCheck now has **seven guides** with consistent cross-navigation across all pages.

### Why this batch

The 2026-08-23 ops-log named "HardenCheck seventh guide" as the top task (a) candidate in multiple consecutive entries. The specific suggestion (HSTS preload) was already covered by `enable-hsts-header.html`, so the Referrer-Policy guide was chosen as the highest-value untouched topic: it is a header HardenCheck flags, opens a new search cluster, is genuinely underserved, and has clean scope (one header, eight values, clear recommendation).

### Blocked / not done
- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates
- **Task (a) — InvoiceQuick fourth guide** — InvoiceQuick has 3 guides (fewest in the suite). "How to write a freelance contract" targeting "freelance contract template", "freelance service agreement", "independent contractor agreement" — high-intent for InvoiceQuick's core audience (freelancers). Named as next candidate in 2026-08-23 entry.
- **Task (d) — LeakCheck, ExposureCheck, or HardenCheck launch posts** — DepCheck (2026-08-01) and ComplyKit (2026-08-05) have launch posts prepared. LeakCheck, ExposureCheck, and HardenCheck do not. HardenCheck is the top task (d) candidate given it now has 7 guides (strong content foundation for a launch).

---

## 2026-08-23

**Batch: SEO article (priority a) — ComplyKit fourth guide: which privacy laws apply to my website (GDPR vs CCPA vs CalOPPA)**

### What shipped

**New page:** `complykit/guides/which-privacy-laws-apply-to-my-website.html`
Live URL: https://labs.copperbaytech.com/complykit/guides/which-privacy-laws-apply-to-my-website.html
Git commit: `c6479db` pushed to `dukotah/complykit` main

**Target queries:** "which privacy laws apply to my website", "do I need to comply with GDPR as a US company", "GDPR vs CCPA US business", "GDPR vs CCPA vs CalOPPA", "what privacy law applies to small business website", "does GDPR apply to US websites", "does GDPR apply to US company with EU customers"

**Why this topic / why high-intent:**
This is the first question anyone asks before getting a privacy policy — and the answer is confusing because the laws are triggered by visitor location, not company location. A US developer often believes GDPR doesn't apply to them. The query "do I need to comply with GDPR as a US company" is extremely common, high-intent (the searcher is about to go do something about it), and under-answered with authoritative, accurate content. ComplyKit is the ideal destination because the next step after reading the guide is generating a compliant policy. ComplyKit had only 3 guides — tied for fewest in the suite with InvoiceQuick — making it the top task (a) candidate alongside HardenCheck.

**Article structure (~1,000 words):**

1. **The four main frameworks in a comparison table** — GDPR, CalOPPA, CCPA/CPRA, COPPA with jurisdiction, who it covers, and size threshold columns. Clear emphasis: all are triggered by visitor location, not company location.
2. **Five-step decision flowchart** — structured as a decision box: (1) is your site publicly accessible? (2) does it collect personal data? → GDPR applies. (3) are US visitors reachable? → CalOPPA applies. (4) do you meet CCPA thresholds? → CCPA/CPRA also applies. (5) do you target children under 13? → COPPA.
3. **GDPR vs CCPA: key differences table** — five rows: lawful basis for processing, "selling" personal data concept, breach notification, subject rights/timeline, and fines.
4. **One policy for all three** — practical guidance that a single document with a general section + GDPR addendum + CCPA addendum satisfies all three laws. How ComplyKit structures output when multiple regions are selected.
5. **ComplyKit CTA** — with ShipSafe callout (demand-letter firms check for missing privacy policy links).
6. **FAQ** — 8 questions with JSON-LD FAQPage schema.
7. **Suite cross-links** — ShipSafe, LeakCheck, HardenCheck, ExposureCheck.

**Nav/sitemap also updated:**
- `complykit/sitemap.xml` — new guide URL added (lastmod: 2026-08-23)
- `complykit/index.html` — "Which laws apply?" link added to header nav + footer nav
- All 3 existing guides (do-i-need-a-privacy-policy, what-should-a-privacy-policy-include, cookie-consent-banner-requirements) — header and footer navs updated to include the new guide

### Why this batch

ComplyKit had 3 guides (tied with InvoiceQuick for fewest in the suite). The 2026-08-21 ops-log entry named a ComplyKit fourth guide as a top task (a) candidate. The "which privacy laws apply" question is categorically distinct from the three existing guides (do I need a policy, what to include, cookie consent) — it addresses a prior, blocking question: before you can write a policy, you need to know which laws apply. It opens a new search cluster around GDPR-as-a-US-company queries, which are extremely common and consistently under-answered.

### Blocked / not done
- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates
- **Task (a) — HardenCheck seventh guide** — "how to implement HSTS correctly (max-age, includeSubDomains, preload)" targeting "hsts preload requirements", "strict-transport-security how to configure". HardenCheck has 6 guides; this was explicitly named as the top task (a) candidate in the 2026-08-21 entry.
- **Task (a) — InvoiceQuick fourth guide** — InvoiceQuick now has the fewest guides (3). "How to write a freelance contract" or "how to write a statement of work" are adjacent to invoicing and high-intent queries in the InvoiceQuick audience.
- **Task (d) — LeakCheck, ExposureCheck, or HardenCheck launch posts** — none of these three tools have launch posts prepared yet in forge/marketing/.

---

## 2026-08-21

**Batch: SEO article (priority a) — ShipSafe fifth guide: do accessibility overlays prevent ADA lawsuits?**
**Also completed: ExposureCheck nav update (deferred task b from 2026-08-19)**

### What shipped

**New page:** `shipsafe/guides/accessibility-overlay-ada-lawsuit.html`
Live URL: https://labs.copperbaytech.com/shipsafe/guides/accessibility-overlay-ada-lawsuit.html
Git commit: `d7b5d1c` pushed to `dukotah/shipsafe` main

**Target queries:** "does accessibe prevent ada lawsuit", "accessibility overlay ada lawsuit", "do accessibility overlays work legally", "accessiBe ADA lawsuit", "does accessibility widget prevent lawsuit", "does userway prevent ada lawsuit", "accessibility overlay legal protection"

**Why this topic / why high-intent:**
Hundreds of thousands of businesses pay $50–$500/month for accessibility overlay products (accessiBe, UserWay, AudioEye, EqualWeb) expecting legal protection from ADA website lawsuits. The reality — that overlays do not reliably prevent lawsuits and that courts evaluate actual WCAG 2.1 AA conformance via real AT testing, not widget presence — is underserved as an SEO article. Business owners who just received a demand letter or are evaluating overlay products are actively searching these exact queries. ShipSafe's last guide shipped on 2026-07-31 (the ADA demand letter response guide), making it the most overdue tool for a new guide in the suite.

**Article structure (~1,000 words):**

1. **The short answer box** — overlays don't reliably prevent lawsuits; courts evaluate actual accessibility, not product subscriptions. No overlay vendor certificate has legal weight.
2. **What overlays actually do** — technical explainer: JS widget, runs after page load, injects ARIA, adjusts contrast at render time, provides a preference toolbar.
3. **Why they don't provide legal protection** — ADA Title III standard is actual user access; courts use expert witness AT testing + plaintiff testimony; the National Federation of the Blind and hundreds of accessibility professionals have formally opposed overlay products as harmful to screen reader users.
4. **What overlays cannot fix** (two-column table): can't fix → missing alt text, unlabeled form inputs, broken keyboard nav in custom JS components, focus trap bugs, heading hierarchy, ARIA roles. May help with → visual preference controls (font size, line spacing) for sighted users with low vision.
5. **The compliance certificate problem** — vendor certificates carry no legal standing; displaying an inaccurate badge may be an aggravating factor; courts measure WCAG 2.1 AA via expert testing.
6. **What actually reduces risk** — checklist: code-level WCAG remediation, git-documented fixes, keyboard testing, accessibility statement, quarterly audits.
7. **If you already have one installed** — don't remove immediately; audit independently, fix in code, test with VoiceOver/NVDA for conflicts.
8. **ShipSafe CTA** — scans live HTML the way automated plaintiff scanners do, with or without overlay running.
9. **FAQ** — 8 questions with JSON-LD FAQPage schema.
10. **Suite cross-links** — ComplyKit, HardenCheck, LeakCheck, ExposureCheck.

**Nav/sitemap also updated:**
- `shipsafe/sitemap.xml` — new guide URL added (lastmod: 2026-08-21)
- `shipsafe/index.html` — "Overlays" link added to header nav + "Overlays & lawsuits" to footer nav
- All 4 existing guides (wcag-compliance-ai-generated-sites, wcag-2-1-vs-2-2-what-changed, ada-compliance-checklist, ada-demand-letter-response) — header and footer navs updated to include the new overlay guide

**Also shipped (deferred task b from 2026-08-19):**
ExposureCheck nav update — "Pre-launch audit" link added to header and footer nav of index.html and all 3 existing guide pages.
Git commit: `a9d5175` pushed to `dukotah/exposurecheck` main

Files changed:
- `exposurecheck/index.html` — header + footer nav updated
- `exposurecheck/guides/fix-exposed-env-file.html` — header + footer nav updated
- `exposurecheck/guides/how-to-check-website-exposed-files.html` — header + footer nav updated
- `exposurecheck/guides/prevent-exposed-files-website.html` — header + footer nav updated

### Why this batch

ShipSafe had 4 guides; all other tools in the suite now have 4–6. ShipSafe's last guide (demand letter response) shipped 2026-07-31. The accessibility overlay topic is exactly the kind of high-intent, financially-consequential question that business owners search immediately before or after spending money on a product that doesn't solve their legal problem — and no clear, accurate explainer exists at the query "does accessibe prevent ada lawsuit."

### Blocked / not done
- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates
- **Task (a) — HardenCheck seventh guide** — HardenCheck has 6 guides; a "how to implement HSTS correctly (max-age, includeSubDomains, preload)" guide would target the query "hsts preload requirements" / "strict-transport-security how to configure". High-intent developer search.
- **Task (a) — ComplyKit fourth guide** — ComplyKit has 3 guides. A "GDPR vs CCPA vs CalOPPA: which privacy law applies to my website" guide would target the query "do I need to comply with GDPR as a US company", "what privacy laws apply to my website". High-intent for the exact audience ComplyKit serves.
- **Task (d) — LeakCheck launch posts** — `forge/marketing/launch-leakcheck.md` not yet written. LeakCheck, ExposureCheck, and HardenCheck have no launch posts prepared yet.

---

## 2026-08-19

**Batch: SEO article (priority a) — ExposureCheck fourth guide: pre-launch exposed files audit**

### What shipped

**New page:** `exposurecheck/guides/pre-launch-exposed-files-audit.html`
Live URL: https://labs.copperbaytech.com/exposurecheck/guides/pre-launch-exposed-files-audit.html
Git commit: `3d5481d` pushed to `dukotah/exposurecheck` main

**Target queries:** "exposed files website check before launch", "pre-launch security checklist exposed files", "how to find exposed files web server before go-live"

**Why ExposureCheck / why this topic:**
ExposureCheck had only 3 guides — the fewest in the suite — and was explicitly named the "top task (a) candidate" in the 2026-08-17 ops-log entry. The pre-launch audit scenario is categorically distinct from the 3 existing guides (fix, detect, prevent): it targets the staging window BEFORE a site goes live.

**Article contents:**
1. Why the staging window matters (scanners index within hours)
2. 10 check categories with specific paths (env files, .git, DB dumps, config backups, private keys, logs, source maps, dir listing, .DS_Store, admin paths)
3. HTTP status code table (200/403/404)
4. Manual curl verification commands
5. Server config blocks (nginx, Apache .htaccess, Vercel vercel.json, Netlify _redirects)
6. Build artifact checklist (source maps, .env not in dist/, node_modules, embedded vars)
7. ExposureCheck 2-stage workflow (staging + post-launch scan)
8. Cross-link to LeakCheck for code-level secret scanning
9. 6-step pre-launch workflow with HardenCheck as step 6
10. Suite grid: LeakCheck, HardenCheck, DepCheck, ShipSafe
11. FAQ with 6 questions (JSON-LD FAQPage schema)

**Files changed:**
- `exposurecheck/guides/pre-launch-exposed-files-audit.html` — new guide (created)
- `exposurecheck/sitemap.xml` — new guide URL added (lastmod: 2026-08-19)

**Deferred:** Nav link to new guide not yet added to existing pages (index.html, fix-exposed-env-file, how-to-check-website-exposed-files, prevent-exposed-files-website) — deferred to next task (b) pass.

### Blocked / not done
- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log.
- **Auto-posting** — forbidden.

### Next candidates
- **Task (b) — ExposureCheck nav update** — add "Pre-launch audit" link to index.html and all 3 existing guide pages. Small task, ~4 file edits.
- **Task (a) — ShipSafe fifth guide** — "does accessibility overlay prevent ADA lawsuit" / "accessiBe ADA compliant". High-intent, timely. ShipSafe has 4 guides, all other tools now have 4+.

---

## 2026-08-17

**Batch: On-page SEO + cross-linking (priority b) — three remaining cross-tool body-text links**

### What shipped

Three inline body-text cross-links added across three repos. These were the specific links flagged as "next candidates" in every ops-log entry since 2026-08-09 (three task-a runs deferred them). All three are editorial links embedded in article body text at the natural point in the reader's journey — not just nav/footer links, which already existed on all pages.

| File changed | Link added | Natural reader moment |
|---|---|---|
| `leakcheck/guides/prevent-committing-secrets-pre-commit.html` | → DepCheck | In the "What the hook doesn't cover" table + paragraph: new row explaining that supply-chain attacks (malicious npm packages) bypass pre-commit hooks, with DepCheck named as the dependency-audit layer in the complete prevention stack |
| `exposurecheck/guides/fix-exposed-env-file.html` | → HardenCheck | In the "Prevention: stop this from happening again" checklist: new bullet noting that HTTP security headers (X-Content-Type-Options, CSP, X-Frame-Options) are a complementary hardening layer separate from fixing file exposure, with HardenCheck as the free header-audit tool |
| `shipsafe/guides/ada-demand-letter-response.html` | → ComplyKit | In the "Establish an ongoing accessibility posture" checklist: new bullet noting that plaintiff firms frequently check for missing privacy policies alongside WCAG failures (CCPA, CalOPPA, GDPR bundling), with ComplyKit as the free policy generator |

**Git commits (one per repo, all pushed to main):**
- `leakcheck`: `5c8b631`
- `exposurecheck`: `0cd8710`
- `shipsafe`: `334da6e`

### Why this batch

Task (b) has been listed as "top task (b) candidate" in the three consecutive ops-log entries since 2026-08-09 without being executed (2026-08-11, 2026-08-13, 2026-08-15 all ran task-a guides instead). These three specific links were named explicitly in all three. Task (a) is described as "top priority" but the rotation was overdue for a (b) run.

**Insertion logic — why each link is at the right moment:**

1. **LeakCheck prevention → DepCheck** — the "What the hook doesn't cover" table is the exactly right location: it's the section where the guide explains the hook's precise scope and what each gap needs. A pre-commit hook scans staged diffs of YOUR code; it cannot scan the npm packages you install. A compromised or typosquatted package (`node_modules/` is never committed) bypasses the hook entirely. DepCheck is the right tool for that. The paragraph-level mention ("the complete prevention stack") gives DepCheck equal standing alongside ExposureCheck in the named stack — editorially the strongest placement possible.

2. **ExposureCheck fix → HardenCheck** — the "Prevention: stop this from happening again" list is the section where the guide moves from "fix the immediate problem" to "harden your posture." File exposure (`.env` accessible at a URL) is a server-configuration gap; HTTP security headers are a separate class of gap. Both are things a security scanner checks. The connection is accurate and genuinely useful: a developer who just fixed an `.env` exposure is exactly the person who should also check their headers, because both gaps appear on the same security audit report.

3. **ShipSafe demand letter → ComplyKit** — the "ongoing posture" checklist is where the guide moves from "respond to this incident" to "prevent the next one." The CCPA/CalOPPA bundling point is real and documented: plaintiff firms that automate WCAG scanning commonly also check for missing privacy policies (which CCPA, CalOPPA, and GDPR require independently). Adding this as a checklist item (not just a suite card at the bottom) gives it editorial weight and creates a body-text link from ShipSafe to ComplyKit that the existing suite section doesn't provide.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates

- **Task (a) — ExposureCheck fourth guide** — ExposureCheck has 3 guides (fix, detect, prevent). A "how to audit a website for exposed files before launch" pre-launch sweep guide — distinct from the existing "how to check website exposed files" (which covers using the tool) — targets the pre-launch moment for developers doing a systematic check before handing off a site. Query: "exposed files website check before launch", "how to find exposed files web server before go-live". Top task (a) candidate.
- **Task (a) — LeakCheck or HardenCheck continuation** — LeakCheck has 5 guides, HardenCheck has 6, DepCheck has 4. ExposureCheck has 3, making it the tool with the fewest guides in the suite. ExposureCheck is the top task (a) candidate for the next SEO content run.

---

## 2026-08-15

**Batch: SEO content (priority a) — DepCheck fourth guide: npm package license compliance**

### What shipped

**New page:** `depcheck/guides/npm-license-compliance.html`
Live URL: https://labs.copperbaytech.com/depcheck/guides/npm-license-compliance.html
Git commit: `53e086c` pushed to `dukotah/depcheck` main

A ~900-word guide targeting the queries **"npm license compliance"**, **"check npm package licenses"**, **"AGPL npm package commercial"**, **"open source license checker npm"**, **"GPL npm project"**, **"npm license check"** — aimed at a developer or engineering manager who has seen the words "AGPL" or "copyleft" in a scan result and needs a concrete answer to "does this actually affect my project?"

**Eyebrow:** "License compliance guide" — fourth distinct guide category alongside Fix (CVE remediation), Audit (pre-deploy security sweep), and Typosquatting (supply-chain attack detection). License compliance is categorically different from all three: it is not a security vulnerability, it is a legal obligation — and `npm audit` has no knowledge of it at all.

**Audience distinction:** The three existing DepCheck guides all target "security vulnerability" searches (CVE, exploit, malicious code). The license compliance guide opens a new, non-overlapping search cluster: engineering managers, startup founders, and developers doing compliance due diligence — people searching because they found "copyleft" in a scan result, are undergoing M&A due diligence, or need to answer a customer security questionnaire. This is a new audience the other guides don't reach.

**Article structure (TechArticle + FAQPage JSON-LD):**

1. **Why license compliance is a separate audit from CVE scanning** — explains the fundamental difference: a CVE is a bug (someone publishes a patch, you upgrade, risk closes); a license is a permanent legal condition. No advisory database tracks it. No future release fixes it. The consequence is a legal obligation, not a vulnerability score.

2. **The npm license landscape** — a table covering nine license types across four columns (license name, type, commercial use safety, key obligation):
   - Safe/permissive: MIT, ISC, BSD-2, BSD-3; Apache-2.0 (with patent grant note); CC0/Unlicense
   - Use with care: LGPL-2.1/3.0 (dynamic linking caveat + JavaScript bundler grey area)
   - Risky: GPL-2.0/3.0 (distribution trigger); AGPL-3.0 (network-use trigger — the web app problem); SSPL-1.0 (non-OSI, full infrastructure requirement); EUPL-1.2 (EU copyleft)
   - No rights: Unlicensed / no license field (legally "all rights reserved")

3. **The AGPL blind spot — especially for AI/ML projects** — the key distinction between GPL (triggered by distribution) and AGPL (triggered by network service). The AGPL paragraph: "for a web app — including APIs, SaaS dashboards, and any backend service with external users — AGPL means: if you use even one AGPL-licensed npm package, you must make your entire application's source code available to users under the AGPL license." Explicit callout box warning about AI/ML libraries: many HuggingFace, model-serving, and LLM client libraries in the npm ecosystem carry AGPL. M&A due-diligence angle: AGPL in a target company's dependency tree is a common deal complication.

4. **How to check all your npm package licenses** — three graduated methods:
   - Step 1: Quick sweep of direct dependencies via DepCheck paste (the no-install path, runs in browser)
   - Step 2: Single-package spot-check with `npm info package-name license` and `npm pack package-name` + reading the LICENSE file in the tarball (plus the caveat: the `license` field is self-reported and can be wrong)
   - Step 3: Full transitive audit with `license-checker --production` — walk the entire `node_modules` tree, JSON output, `--onlyAllow` flag for CI gating

5. **Transitive dependencies: the hidden problem** — explains the mechanism with a concrete scenario (an MIT-licensed utility package adds a feature in a minor release that depends on an AGPL library; your lockfile updates, AGPL enters your production bundle without any change to your own `package.json`). Tip callout: commit `package-lock.json` and run `license-checker --production` in CI on every PR as a build gate.

6. **What to do when you find a risky license** — four options in order of preference:
   - Find a permissively-licensed alternative (search npmjs.com, check download count and maintenance health)
   - Buy a commercial license (the dual-licensing model; common in infrastructure and AI tooling)
   - Isolate via a service boundary (the microservice escape hatch; legally uncertain, treat as last resort)
   - Open-source under the copyleft license (for internal tools, dev tooling, or projects where code is not a competitive secret)
   - Unlicensed packages: contact the author and ask for a license (most will add MIT if asked)

7. **FAQ (7 questions):** Does npm audit check licenses? What's the difference between AGPL and GPL for a web app? Does bundling LGPL with webpack trigger copyleft? Can I use AGPL for an internal tool? What does "SEE LICENSE IN LICENSE.md" mean? Is no-license-field safe? How do I check transitive licenses?

**Also updated in this run:**
- `depcheck/sitemap.xml` — new guide URL added at priority 0.8, lastmod 2026-08-15
- `depcheck/index.html` — header nav: added "Licenses" link; footer nav: added "License guide" link
- `depcheck/guides/fix-npm-audit-vulnerabilities.html` — header nav: added "Licenses" link; footer nav: added "License guide" link
- `depcheck/guides/audit-npm-packages-security.html` — header nav: added "Licenses" link; footer nav: added "License guide" link
- `depcheck/guides/npm-typosquatting-how-to-detect.html` — header nav: added "Licenses" link; footer nav: added "License guide" link

DepCheck now has four guides with consistent cross-navigation across all pages.

### Why this batch

The ops-log explicitly identified "npm package license compliance" as a top task (a) candidate in the 2026-08-13 entry, alongside an ExposureCheck pre-audit sweep guide. License compliance was chosen over the ExposureCheck guide for two reasons: (1) it opens a new, non-overlapping audience (legal/compliance searches) rather than extending the same developer-security cluster ExposureCheck already serves; (2) DepCheck already checks licenses in its tool, making this an honest extension of the tool's existing value proposition.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked: `contact@copperbaytech.com` email delivery not confirmed in ops-log. No new CTAs added. Existing CTAs untouched.
- **Auto-posting** — forbidden.

### Next candidates

- **Task (b) — three remaining cross-tool SEO links** — these have been pending since the 2026-08-09 cross-linking run. Three specific inline body-text links not yet executed: (1) LeakCheck guides → DepCheck in supply-chain context; (2) ExposureCheck guides → HardenCheck at the point where security headers would have prevented .env access via misconfigured paths; (3) ShipSafe ADA demand letter guide → ComplyKit at the "bundle CCPA allegation" callout. Top task (b) candidate for the next run.
- **Task (a) — ExposureCheck fourth guide** — ExposureCheck still has 3 guides (fix, detect, prevent). Query: "exposed files website check before launch", "how to find exposed files web server". Top task (a) candidate for the next SEO content run.

---

## 2026-08-13

**Batch: SEO content (priority a) — LeakCheck fifth guide: how to permanently remove a secret from git history**

### What shipped

**New page:** `leakcheck/guides/remove-secret-from-git-history.html`
Live URL: https://labs.copperbaytech.com/leakcheck/guides/remove-secret-from-git-history.html
Git commit: `b191c9b` pushed to `dukotah/leakcheck` main

A ~950-word comprehensive guide targeting the queries **"remove secret from git history"**, **"git filter-repo remove password"**, **"BFG repo cleaner remove secret"**, **"delete api key from git history permanently"**, **"remove sensitive data from git history"**.

**Eyebrow:** "History rewrite guide" — the fifth distinct guide category in LeakCheck (Fix, Detection, Prevention, Audit, History rewrite).

**Article structure:** Why git rm isn't enough; before you start checklist; tool choice (git-filter-repo vs BFG); git-filter-repo full workflow; BFG workflow including HEAD-protection gotcha; after the force-push (collaborator re-clone, GitHub object cache, fork problem); verification workflow; five common mistakes; FAQ (7 questions).

**Also updated:** sitemap.xml, index.html, and all 4 existing guide footer navs.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **Task (b) — remaining cross-tool SEO links** — three specific links flagged in the 2026-08-11 ops-log still not executed.
- **Task (a) — ExposureCheck or DepCheck next guide** — "how to find exposed files on a website" or "npm package license compliance".

---

## 2026-08-11 (second run — SEO content)

**Batch: SEO content (priority a) — HardenCheck sixth guide: CSP for React and Vite/SPA apps**

### What shipped

**New page:** `hardencheck/guides/csp-react-spa-app.html`
Live URL: https://labs.copperbaytech.com/hardencheck/guides/csp-react-spa-app.html
Git commit: `7c7767c` pushed to `dukotah/hardencheck` main

A ~950-word fix-intent guide targeting the queries **"CSP React app"**, **"Content-Security-Policy Vite"**, **"React security headers"**, **"React CSP unsafe-eval"**, **"CSS-in-JS CSP violation"**.

**Eyebrow:** "React / SPA guide" — sixth guide category. Article covers: why SPAs are harder for CSP; the three violations you'll hit immediately; production starting policy; handle the bundler's inline script; dev mode is different; CSS-in-JS nonces; deployment snippets; FAQ (7 questions).

**Also updated:** index.html and all 5 existing guide header/footer navs; sitemap.xml.

HardenCheck now has six guides with consistent cross-navigation across all pages.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **LeakCheck fifth guide (task a)** — "how to permanently remove a secret from git history using git filter-repo and BFG".
- **Task (b) continuation** — three specific links still flagged from the 2026-08-09 run.

---

## 2026-08-09

**Batch: On-page SEO + cross-linking (priority b) — first task (b) run ever**

### What shipped

**5 inline cross-tool body-text links added across 5 repos.** This is the first task (b) run in the ops log.

| File changed | Link added | Natural reader moment |
|---|---|---|
| `leakcheck/guides/find-hardcoded-api-keys-in-codebase.html` | → ExposureCheck | After rotating a found key: check whether it's also publicly accessible through the live server |
| `depcheck/guides/audit-npm-packages-security.html` | → LeakCheck | In the pre-deploy checklist: scanning OWN source code for hardcoded credentials |
| `invoicequick/guides/create-free-invoice-pdf.html` | → ComplyKit | After the tool CTA: if your site collects client data, GDPR and CalOPPA require a privacy policy |
| `shipsafe/guides/ada-demand-letter-response.html` | → HardenCheck | In the "Establish an ongoing posture" section: HTTP security headers are a complementary layer |
| `complykit/guides/do-i-need-a-privacy-policy.html` | → ShipSafe | In the "ADA demand letters" consequence bullet |

**Git commits:** leakcheck `fb4c4b9`, depcheck `e6678de`, invoicequick `51ec1f2`, shipsafe `d2bb403`, complykit `183b4d2`

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **HardenCheck new guide (task a)** — "what CSP headers should I set for a React/SPA app".
- **Cross-tool SEO sweep continuation (task b)** — more opportunities remain: LeakCheck → DepCheck in supply-chain context; ExposureCheck → HardenCheck; ShipSafe → ComplyKit inline.

---

## 2026-08-07

**Batch: SEO content (priority a) — LeakCheck fourth guide: how to find hardcoded API keys in a codebase**

### What shipped

**New page:** `leakcheck/guides/find-hardcoded-api-keys-in-codebase.html`
Live URL: https://labs.copperbaytech.com/leakcheck/guides/find-hardcoded-api-keys-in-codebase.html
Git commits: `de6ca72`, `022a4c2`, `307b0da` pushed to `dukotah/leakcheck` main

A ~900-word working-tree audit guide targeting the queries **"how to find hardcoded API keys in code"**, **"find hardcoded secrets in codebase"**, **"find api keys in source code"**, **"audit code for hardcoded credentials"**.

**Article structure:** Five hiding places beyond .env; LeakCheck browser paste; grep/ripgrep patterns; gitleaks --no-git; Semgrep p/secrets; comparison table; found one — what now; stop the next one; FAQ (7 questions).

**Also updated:** sitemap.xml, index.html, and all 3 existing guide footer navs.

LeakCheck now has four guides with consistent cross-navigation across all pages.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **Cross-tool SEO audit (task b)** — first task (b) run.
- **HardenCheck new guide (task a)** — "what CSP headers should I set for a React/SPA app".

---

## 2026-08-05

**Batch: Task (d) — ComplyKit launch posts**

### What shipped

**New file:** `forge/marketing/launch-complykit.md`

One-paste-ready launch posts for ComplyKit across seven channels plus AlternativeTo/SaaSHub listing copy, timing notes, and a what-to-log section.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **On-page SEO audit (task b)** — cross-tool internal linking sweep.
- **New guide (task a)** — LeakCheck or HardenCheck.

---

## 2026-08-03

**Batch: SEO content (priority a) — InvoiceQuick third guide: how to invoice a client in another country**

### What shipped

**New page:** `invoicequick/guides/invoice-clients-international.html`
Live URL: https://labs.copperbaytech.com/invoicequick/guides/invoice-clients-international.html
Git commit: `1259370` pushed to `dukotah/invoicequick` main

A ~900-word international invoicing guide targeting the queries **"how to invoice clients in another country"**, **"international invoice freelancer"**, **"invoice foreign client"**.

**Also updated:** sitemap.xml, index.html, and both existing guide navs.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **ComplyKit → launch posts (task d)**.
- **On-page SEO audit (task b)**.

---

## 2026-08-01

**Batch: Task (d) — DepCheck launch posts**

### What shipped

**New file:** `forge/marketing/launch-depcheck.md`

One-paste-ready launch posts for DepCheck across seven channels plus AlternativeTo/SaaSHub listing copy, timing notes, and a what-to-log section.

### Blocked / not done

- **Task (c) — new mailto: CTAs** — still blocked.
- **Auto-posting** — forbidden.

### Next candidates

- **ComplyKit → launch posts (task d)**.
- **InvoiceQuick → third guide (task a)**.

---
